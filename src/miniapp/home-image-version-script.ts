export const HOME_IMAGE_VERSION_SCRIPT = `
(function(){
  // Home intro image now has exactly one public display path:
  // /app/api/home-intro-image.png
  // No meta endpoint, localStorage version cache, or JS image injection is needed.
})();
`;