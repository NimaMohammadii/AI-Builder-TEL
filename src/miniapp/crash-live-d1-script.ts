export const CRASH_LIVE_D1_SCRIPT = `
(function(){
  var UNIT=1000000000;
  var cache=null, lastSyncAt=0, syncing=false, syncQueued=false, lastRendered='', syncTimer=0, localSeq=1;
  function ton(n){var v=Math.max(0,Math.floor(Number(n)||0))/UNIT;return v.toFixed(4).replace(/\.0+$/,'').replace(/(\.\d*?)0+$/,'$1')}
  function betTon(n,isVirtual){var v=Math.max(0,Math.floor(Number(n)||0))/UNIT;if(!isVirtual)return ton(n);if(Math.abs(v-Math.round(v))<.000001)return String(Math.round(v));if(Math.abs(v*10-Math.round(v*10))<.000001)return String(Math.round(v*10)/10);return String(Math.round(v*100)/100).replace(/\.0+$/,'')}
  function payoutOf(b){var payout=Number(b&&b.payoutNano)||0;if(payout>0)return Math.max(0,Math.floor(payout));var m=Number(b&&b.cashoutMultiplier)||0,amount=Number(b&&b.amountNano)||0;return m>0&&amount>0?Math.max(0,Math.floor(amount*m)):0}
  function statusRank(b){if(b&&b.status==='bet')return 0;if(b&&b.status==='cashout')return 1;if(b&&b.status==='crashed')return 2;return 3}
  function touchValue(b){var v=Number(b&&b.localOrder)||0;if(v)return v;var t=Date.parse(String((b&&b.createdAt)||(b&&b.updatedAt)||''));return Number.isFinite(t)?t:0}
  function betOrder(b){var v=Number(b&&b.virtualOrder)||0;if(v)return v;return touchValue(b)}
  function sortLiveBets(bets){return bets.slice().sort(function(a,b){var ao=betOrder(a),bo=betOrder(b);if(ao!==bo)return ao-bo;var ar=statusRank(a),br=statusRank(b);if(ar!==br)return ar-br;return String(a&&a.user||'').localeCompare(String(b&&b.user||''))})}
  function keyOf(roundId,userId){return String(roundId||'')+'::'+String(userId||'')}
  function mergeBet(bet){if(!bet||!bet.roundId||!bet.userId)return;var bets=(cache&&cache.bets)?cache.bets.slice():[];var k=keyOf(bet.roundId,bet.userId),found=false;for(var i=0;i<bets.length;i++){if(keyOf(bets[i].roundId,bets[i].userId)===k){bets[i]=Object.assign({},bets[i],bet);found=true;break}}if(!found)bets.unshift(bet);if(!cache)cache={ok:true,totalNano:0,bets:[],state:null,nextSyncMs:900};cache.bets=bets;cache.totalNano=bets.reduce(function(s,b){return s+Number(b&&b.amountNano||0)},0);render(cache)}
  function markRoundCrashed(roundId){if(!cache||!cache.bets||!roundId)return;var changed=false,bets=cache.bets.map(function(b){if(Number(b&&b.roundId)===Number(roundId)&&b.status==='bet'){changed=true;return Object.assign({},b,{status:'crashed',localOrder:Date.now()+localSeq++})}return b});if(changed){cache=Object.assign({},cache,{bets:bets});render(cache)}}
  function amountFor(roundId,userId,payout,m){var bets=(cache&&cache.bets)||[],k=keyOf(roundId,userId);for(var i=0;i<bets.length;i++)if(keyOf(bets[i].roundId,bets[i].userId)===k)return Number(bets[i].amountNano)||0;return payout&&m>0?Math.floor(payout/m):0}
  function nano(v){return Math.max(0,Math.floor((Number(String(v||'').replace(',','.'))||0)*UNIT))}
  function tgUser(){var tg=window.Telegram&&window.Telegram.WebApp;var u=tg&&tg.initDataUnsafe&&tg.initDataUnsafe.user;var id=String((u&&u.id)||localStorage.getItem('ownerId')||'local').trim();var name=u?String(u.first_name||u.last_name||'User'):'You';return{id:id||'local',name:name||'User'}}
  function cleanText(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}
  function isCrashActive(){var active=document.querySelector('.view.active');return !!(active&&active.id==='crash')}
  function clearSyncTimer(){if(syncTimer){clearTimeout(syncTimer);syncTimer=0}}
  function scheduleNextSync(j){clearSyncTimer();if(!j||!j.ok||!isCrashActive())return;var delay=Math.max(250,Math.min(12000,Number(j.nextSyncMs)||1200));syncTimer=setTimeout(function(){syncTimer=0;requestSync(true)},delay)}
  async function post(path,body){try{return await fetch(path,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body),keepalive:true}).then(function(r){return r.json().catch(function(){return null})})}catch(e){return null}}
  async function pull(force){try{var j=await fetch('/app/api/crash-live',{cache:'no-store'}).then(function(r){return r.json().catch(function(){return null})});if(j&&j.ok){cache=j;render(j);scheduleNextSync(j)}}catch(e){}}
  function requestSync(force){var now=Date.now();if(!isCrashActive()&&!force)return Promise.resolve();if(!force&&now-lastSyncAt<900)return Promise.resolve();if(syncing){syncQueued=Boolean(force||syncQueued);return Promise.resolve()}syncing=true;lastSyncAt=now;return pull(force).finally(function(){syncing=false;if(syncQueued){syncQueued=false;requestSync(true)}})}
  function render(j){var list=document.getElementById('crashLiveList'),total=document.getElementById('crashLiveTotal');if(!list||!j||!j.ok)return;if(total)total.textContent=ton(j.totalNano)+' TON';var bets=sortLiveBets(j.bets||[]);var html='';if(!bets.length)html='<div class="crash-live-empty">No bets yet</div>';else html=bets.map(function(b){var cash=b.status==='cashout';var crash=b.status==='crashed';var betAmount=betTon(Number(b&&b.amountNano||0),b.isVirtual)+' TON';var amount=(cash?'<span class="crash-live-plus">+</span>':'')+cleanText(betAmount);var status=cash||crash?'':'Bet';var cls=cash?'cashout':(crash?'crashed':'');return '<div class="crash-live-row '+cls+'"><span class="crash-live-user">'+cleanText(b.user)+'</span><span class="crash-live-amount">'+amount+'</span><span class="crash-live-status">'+status+'</span></div>'}).join('');if(html!==lastRendered){list.innerHTML=html;lastRendered=html}}
  function bindDrawer(){var box=document.getElementById('crashLive'),btn=document.getElementById('crashLiveToggle');if(!box||!btn)return;btn.onclick=function(){var open=!box.classList.contains('open');box.classList.toggle('open',open);btn.setAttribute('aria-expanded',open?'true':'false')}}
  function bind(){bindDrawer();document.addEventListener('click',function(ev){var btn=ev.target&&ev.target.closest&&ev.target.closest('button');if(!btn)return;var u=tgUser();if(btn.getAttribute('data-view')==='crash'||btn.getAttribute('data-game-view')==='crash')setTimeout(function(){requestSync(true)},120);if(btn.id==='crashStart'){setTimeout(function(){var start=document.getElementById('crashStart');if(!start||start.textContent!=='Bet Placed')return;var amount=nano((document.getElementById('crashAmount')||{}).value);post('/app/api/crash-live/bet',{userId:u.id,username:u.name,amountNano:amount}).then(function(){requestSync(true)})},60)}},true);window.addEventListener('vexa-crash-cashout',function(ev){var u=tgUser(),d=ev.detail||{};var rid=Number(d.roundId)||0;var m=Number(d.multiplier)||1;var payout=Number(d.payoutNano)||0;if(!rid)return requestSync(true);var amount=amountFor(rid,u.id,payout,m);mergeBet({roundId:rid,userId:u.id,user:u.name,amountNano:amount,status:'cashout',cashoutMultiplier:m,payoutNano:payout,isVirtual:false,updatedAt:new Date().toISOString(),localOrder:Date.now()+localSeq++});post('/app/api/crash-live/cashout',{roundId:rid,userId:u.id,multiplier:m}).then(function(r){if(r&&r.ok)mergeBet({roundId:rid,userId:u.id,user:u.name,status:'cashout',cashoutMultiplier:m,payoutNano:Number(r.payoutNano)||payout,isVirtual:false,updatedAt:new Date().toISOString(),localOrder:Date.now()+localSeq++});requestSync(true)})});window.addEventListener('vexa-round-ended',function(ev){var d=ev&&ev.detail||{};markRoundCrashed(Number(d.roundId)||0);requestSync(true)});window.addEventListener('focus',function(){requestSync(false)});document.addEventListener('visibilitychange',function(){if(document.visibilityState==='visible')requestSync(false)});if(window.MutationObserver){var root=document.getElementById('crash');if(root)new MutationObserver(function(){if(root.classList.contains('active'))requestSync(true);else clearSyncTimer()}).observe(root,{attributes:true,attributeFilter:['class']})}if(isCrashActive())requestSync(true)}
  window.VexaCrashLiveD1={load:function(){return requestSync(true)},postBet:function(amountNano){var u=tgUser();return post('/app/api/crash-live/bet',{userId:u.id,username:u.name,amountNano:amountNano}).then(function(r){requestSync(true);return r})},postCashout:function(roundId,multiplier){var u=tgUser();return post('/app/api/crash-live/cashout',{roundId:roundId,userId:u.id,multiplier:multiplier}).then(function(r){requestSync(true);return r})},refresh:function(){return requestSync(true)}};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();
})();
`;