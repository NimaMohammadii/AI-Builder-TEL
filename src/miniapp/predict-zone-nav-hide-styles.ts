export const PREDICT_ZONE_NAV_HIDE_STYLES = `
body.predict-zone-open .tabs{display:none!important}
body.predict-zone-open .content{padding-bottom:calc(18px + env(safe-area-inset-bottom))!important}
`;
