export const SECTION_LOCK_IMAGE_SPLIT_SCRIPT = `
(function(){
  // Section lock images are now handled by SECTION_LOCK_SCRIPT.
  // This script is intentionally kept as a no-op to avoid duplicate polling.
})();
`;
