export const PREDICT_EXTRA_MARKETS_SCRIPT = `
(function(){
  function ready(fn){if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',fn);else fn();}
  ready(function(){
    var root=document.getElementById('predictzone');
    if(!root||root.dataset.extraMarketsReady==='1')return;
    root.dataset.extraMarketsReady='1';
    var menu=root.querySelector('.predict-zone-category-menu');
    var originalCard=root.querySelector('[data-predict-card]');
    var originalSheet=root.querySelector('[data-predict-bet-sheet]');
    if(!menu||!originalCard||!originalSheet)return;
    menu.innerHTML=''+
      '<button type="button" class="predict-zone-category-card active" data-vexa-predict-market="bitcoin"><span>Bitcoin</span></button>'+ 
      '<button type="button" class="predict-zone-category-card" data-vexa-predict-market="ethereum"><span>Ethereum</span></button>'+ 
      '<button type="button" class="predict-zone-category-card" data-vexa-predict-market="solana"><span>Solana</span></button>'+ 
      '<button type="button" class="predict-zone-category-card" data-vexa-predict-market="gold"><span>Gold</span></button>'+ 
      '<button type="button" class="predict-zone-category-card" data-vexa-predict-market="ton"><span>TON</span></button>'+ 
      '<button type="button" class="predict-zone-category-card" data-vexa-predict-market="football"><span>Football</span></button>'+ 
      '<button type="button" class="predict-zone-category-card" data-vexa-predict-market="politics"><span>Politics</span></button>'+ 
      '<button type="button" class="predict-zone-category-card" data-vexa-predict-market="fun"><span>Fun</span></button>';
    var card=originalCard.cloneNode(true);originalCard.parentNode.replaceChild(card,originalCard);
    var sheet=originalSheet.cloneNode(true);originalSheet.parentNode.replaceChild(sheet,originalSheet);
    var tg=window.Telegram&&window.Telegram.WebApp;var tgUser=(tg&&tg.initDataUnsafe&&tg.initDataUnsafe.user)||{};
    var chart=card.querySelector('[data-predict-chart]'),line=chart&&chart.querySelector('.predict-zone-chart-line'),fill=chart&&chart.querySelector('.predict-zone-chart-fill'),dot=chart&&chart.querySelector('.predict-zone-chart-dot'),priceGuide=chart&&chart.querySelector('.predict-zone-price-guide'),axisLabels=chart?chart.querySelectorAll('[data-price-axis]'):[],gridLines=chart?chart.querySelectorAll('[data-chart-grid-line]'):[],question=card.querySelector('[data-predict-question]'),questionImage=card.querySelector('[data-predict-question-image]'),countdown=card.querySelector('[data-predict-countdown]'),live=card.querySelector('.predict-zone-live-price'),start=card.querySelector('.predict-zone-start-price'),resultBox=card.querySelector('[data-predict-result]'),liveBetBox=card.querySelector('[data-predict-live-bets]'),startGuide=chart&&chart.querySelector('[data-predict-start-guide]'),startTarget=chart&&chart.querySelector('[data-predict-start-target]');
    var betTitle=sheet.querySelector('[data-predict-bet-title]'),betQuestion=sheet.querySelector('[data-predict-bet-question]'),betInput=sheet.querySelector('[data-predict-bet-input]'),betUsd=sheet.querySelector('[data-predict-bet-usd]'),betEstimate=sheet.querySelector('[data-predict-bet-estimate]'),betStatus=sheet.querySelector('[data-predict-bet-status]'),betClose=sheet.querySelector('[data-predict-bet-close]'),betSubmit=sheet.querySelector('[data-predict-bet-submit]');
    var markets={bitcoin:{label:'Bitcoin',question:'Bitcoin up or down?',stream:'btcusdt@miniTicker',decimals:0,axisStep:5,axisRange:20,seed:102400,imageUrl:''},ethereum:{label:'Ethereum',question:'Ethereum up or down?',stream:'ethusdt@miniTicker',decimals:2,axisStep:.5,axisRange:5,seed:3600,imageUrl:''},solana:{label:'Solana',question:'Solana up or down?',stream:'solusdt@miniTicker',decimals:2,axisStep:.05,axisRange:.5,seed:160,imageUrl:''},gold:{label:'Gold',question:'Gold up or down?',stream:'paxgusdt@miniTicker',decimals:2,axisStep:.5,axisRange:5,seed:3300,imageUrl:''},ton:{label:'TON',question:'TON up or down?',stream:'tonusdt@miniTicker',decimals:4,axisStep:.005,axisRange:.02,seed:2.85,imageUrl:''}};
    var activeMarket='bitcoin',currentBetSide='up',ws=null,tonWs=null,raf=0,timer=0,liveBetTimer=0,prices=[],currentPrice=0,lastRealPrice=0,realReady=false,axisCenter=0,axisTarget=0,tailY=null,lastRender=0,lastPoint=0,roundStartedAt=Math.floor(Date.now()/300000)*300000,roundStartPrice=0,currentRound=null,historyKey='',shownResultKey='',tonUsdPrice=0,betBusy=false,width=360,height=220,padLeft=14,padRight=78,padY=24;
    function market(){return markets[activeMarket]||markets.bitcoin}function uid(){return String((tgUser&&tgUser.id)||localStorage.getItem('ownerId')||'').trim()}function active(){return root.classList.contains('active')&&document.visibilityState!=='hidden'}function clamp(v,min,max){return Math.max(min,Math.min(max,v))}function roundStep(v,s){return Math.round(v/s)*s}function fmtPrice(v){var m=market(),n=Number(v);if(!Number.isFinite(n)||n<=0)return 'Loading';return '$'+n.toLocaleString('en-US',{minimumFractionDigits:m.decimals,maximumFractionDigits:m.decimals})}function fmtUsd(v){return '$'+Number(v||0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})}function fmtTon(v){return Number(v||0).toLocaleString('en-US',{minimumFractionDigits:0,maximumFractionDigits:4})}function time(ms){var s=Math.max(0,Math.ceil(ms/1000)),m=Math.floor(s/60),r=s%60;return String(m).padStart(2,'0')+':'+String(r).padStart(2,'0')}
    function applyMarketImage(){var m=market();if(!questionImage)return;if(m.imageUrl){questionImage.style.backgroundImage='url("'+String(m.imageUrl).replace(/"/g,'')+'")';questionImage.classList.add('has-image')}else{questionImage.style.backgroundImage='';questionImage.classList.remove('has-image')}}
    function loadMarketImages(){fetch('/app/api/predict-markets',{cache:'no-store'}).then(function(r){return r.json()}).then(function(data){var loaded=data&&data.markets?data.markets:{};Object.keys(loaded).forEach(function(key){if(markets[key]&&loaded[key]&&loaded[key].imageUrl)markets[key].imageUrl=loaded[key].imageUrl});applyMarketImage()}).catch(function(){})}
    function setStatus(t,k){if(!betStatus)return;betStatus.textContent=t||'';betStatus.classList.toggle('bad',k==='bad');betStatus.classList.toggle('good',k==='good')}
    function closeWs(){if(ws){try{ws.onclose=null;ws.onerror=null;ws.onmessage=null;ws.close()}catch(e){}ws=null}}function closeTonWs(){if(tonWs){try{tonWs.onclose=null;tonWs.onerror=null;tonWs.onmessage=null;tonWs.close()}catch(e){}tonWs=null}}
    function connectTon(){if(tonWs||!('WebSocket'in window))return;try{tonWs=new WebSocket('wss://stream.binance.com:9443/ws/tonusdt@miniTicker');tonWs.onmessage=function(e){try{tonUsdPrice=Number(JSON.parse(e.data).c)||tonUsdPrice;updateStake()}catch(x){}};tonWs.onclose=function(){tonWs=null;if(sheet.classList.contains('open')&&active())setTimeout(connectTon,7000)};tonWs.onerror=function(){try{tonWs.close()}catch(e){}}}catch(e){}}
    function connect(){closeWs();var m=market();if(!active()||!m.stream||!('WebSocket'in window))return;try{ws=new WebSocket('wss://stream.binance.com:9443/ws/'+m.stream);ws.onmessage=function(e){try{var p=Number(JSON.parse(e.data).c);if(!p||!isFinite(p))return;lastRealPrice=p;if(activeMarket==='ton')tonUsdPrice=p;if(!realReady){realReady=true;if(chart)chart.classList.add('ready');seed(p);render(0,16)}}catch(x){}};ws.onclose=function(){ws=null;if(active())setTimeout(connect,6000)};ws.onerror=function(){try{ws.close()}catch(e){}}}catch(e){}}
    function clearChart(){prices=[];currentPrice=0;lastRealPrice=0;realReady=false;axisCenter=0;axisTarget=0;tailY=null;if(chart)chart.classList.remove('ready');if(line)line.setAttribute('d','');if(fill)fill.setAttribute('d','');axisLabels.forEach(function(x,i){x.textContent='';x.style.top='';if(gridLines[i])gridLines[i].style.top=''});if(startGuide)startGuide.classList.remove('show');if(startTarget){startTarget.classList.remove('show','above','below');startTarget.textContent=''}}
    function seed(p){prices=[];var step=p>1000?1.8:p>100?.18:.006,wave=p>1000?5:p>100?.5:.018;for(var i=0;i<22;i++)prices.push(p-step*12+i*step+Math.sin(i/2.8)*wave);currentPrice=p;lastPoint=performance.now();axisCenter=roundStep(p,market().axisStep||1);axisTarget=axisCenter;syncRound(true)}
    function scale(delta){var m=market(),range=m.axisRange||50;if(!axisCenter)axisCenter=roundStep(currentPrice||m.seed,m.axisStep||1);axisTarget=roundStep(currentPrice||m.seed,m.axisStep||1);axisCenter+=(axisTarget-axisCenter)*Math.min(.26,(delta||16)/900);return{min:axisCenter-range/2,max:axisCenter+range/2}}
    function y(price,s){return clamp(padY+((s.max-price)/(s.max-s.min))*(height-padY*2),padY,height-padY)}
    function updateEntry(s){if(!startGuide||!startTarget||!roundStartPrice){if(startGuide)startGuide.classList.remove('show');return}if(roundStartPrice<=s.max&&roundStartPrice>=s.min){startGuide.style.top=(y(roundStartPrice,s)/height*100)+'%';startGuide.classList.add('show');startTarget.classList.remove('show','above','below');startTarget.textContent=''}else{startGuide.classList.remove('show');startTarget.classList.add('show');if(roundStartPrice>s.max){startTarget.classList.add('above');startTarget.classList.remove('below');startTarget.textContent='Entry ↑'}else{startTarget.classList.add('below');startTarget.classList.remove('above');startTarget.textContent='Entry ↓'}}}
    function path(points){if(!points.length)return'';var d='M'+points[0].x.toFixed(1)+' '+points[0].y.toFixed(1);for(var i=0;i<points.length-1;i++){var a=points[i],b=points[i+1],mx=(a.x+b.x)/2;d+=' C '+mx.toFixed(1)+' '+a.y.toFixed(1)+' '+mx.toFixed(1)+' '+b.y.toFixed(1)+' '+b.x.toFixed(1)+' '+b.y.toFixed(1)}return d}
    function render(progress,delta){if(!prices.length||!line||!fill)return;var s=scale(delta),right=width-padRight,plot=width-padLeft-padRight,slots=Math.max(22,prices.length+1),step=plot/(slots-1),pts=prices.map(function(v,i){return{x:right-((prices.length-i)+progress)*step,y:y(v,s),value:v}});pts.push({x:right,y:y(currentPrice,s),value:currentPrice});var vis=pts.filter(function(p){return p.x>=0&&p.x<=right});if(vis.length<2)vis=pts.slice(-2);var d=path(vis),first=vis[0],last=vis[vis.length-1],xp=last.x/width*100,yp=last.y/height*100;line.setAttribute('d',d);fill.setAttribute('d',d+' L '+last.x.toFixed(1)+' '+height+' L '+first.x.toFixed(1)+' '+height+' Z');if(dot){dot.style.left=xp+'%';dot.style.top=yp+'%'}if(priceGuide)priceGuide.style.top=yp+'%';axisLabels.forEach(function(l,i){var r=axisLabels.length>1?i/(axisLabels.length-1):0,p=s.max-r*(s.max-s.min),top=(y(p,s)/height*100)+'%';l.style.top=top;l.textContent=fmtPrice(p);if(gridLines[i])gridLines[i].style.top=top});updateEntry(s);if(live)live.textContent=fmtPrice(last.value)}
    function frame(now){if(!active()){stop();return}if(!lastRender)lastRender=now;if(now-lastRender<50){raf=requestAnimationFrame(frame);return}var delta=Math.min(120,now-lastRender);lastRender=now;updateCountdown();if(realReady){currentPrice=currentPrice+(lastRealPrice-currentPrice)*(1-Math.pow(.945,delta/100));if(activeMarket==='ton')tonUsdPrice=currentPrice;var elapsed=now-lastPoint;while(elapsed>=3000){prices.push(currentPrice);if(prices.length>23)prices.shift();lastPoint+=3000;elapsed=now-lastPoint}render(clamp(elapsed/3000,0,1),delta)}raf=requestAnimationFrame(frame)}
    function startEngine(){if(!active())return;startTimer();startLiveBets();connectTon();if(!raf){lastRender=0;raf=requestAnimationFrame(frame)}if(!ws)connect()}function stop(){stopLiveBets();closeWs();closeTonWs();if(raf){cancelAnimationFrame(raf);raf=0}}
    function updateCountdown(){if(!countdown)return;var elapsed=(Date.now()-roundStartedAt)%300000;countdown.textContent=time(300000-elapsed)}function startTimer(){if(timer)return;syncRound(true);timer=setInterval(function(){var slot=Math.floor(Date.now()/300000)*300000;if(slot!==roundStartedAt){roundStartedAt=slot;syncRound(true)}updateCountdown()},1000)}
    function syncBalance(payload){var c=payload&&payload.userControls;if(!c||c.tonBalanceNano===undefined)return;var b=Number(c.tonBalanceNano);if(!Number.isFinite(b))return;if(window.VexaTonBalance&&window.VexaTonBalance.write)window.VexaTonBalance.write(b,0,false);else window.dispatchEvent(new CustomEvent('vexa-ton-balance-game-change',{detail:{tonBalanceNano:b}}))}
    function syncRound(force){fetch('/app/api/predict-round?market='+encodeURIComponent(activeMarket)+'&userId='+encodeURIComponent(uid()),{cache:'no-store'}).then(function(r){return r.json()}).then(function(data){syncBalance(data);var round=data&&data.round;if(!round)return;currentRound=round;if(round.startPrice){roundStartPrice=Number(round.startPrice);if(start)start.textContent=fmtPrice(roundStartPrice)}if(round.remainingMs&&countdown)countdown.textContent=time(round.remainingMs);if(round.startsAt)roundStartedAt=Date.parse(round.startsAt)||roundStartedAt;renderHistory(round);updateStake()}).catch(function(){})}
    function renderHistory(round){if(!resultBox)return;var list=[];(round.userBets||[]).concat(round.recentUserBets||[]).forEach(function(b){if(b&&b.status&&!list.some(function(x){return x.id===b.id}))list.push(b)});list=list.slice(0,20);var key=list.map(function(b){return b.id+':'+b.status+':'+b.payoutTon+':'+b.stakeTon}).join('|');if(key===historyKey)return;historyKey=key;if(!list.length){resultBox.className='predict-zone-result-strip';resultBox.innerHTML='';return}var html='<div class="predict-zone-history-track">';list.forEach(function(b){var st=String(b.status||''),stake=Number(b.stakeTon||0),payout=Number(b.payoutTon||0),side=String(b.side||'').toLowerCase()==='down'?'Down':'Up',kind=st==='won'?'win':st==='lost'?'loss':st==='refunded'?'refund':'active',amount=st==='active'?fmtTon(stake):(st==='lost'?'-':'+')+fmtTon(st==='lost'?stake:(payout||stake));html+='<span class="predict-zone-history-card '+kind+'">'+side+' '+amount+'</span>'});html+='</div>';resultBox.innerHTML=html;resultBox.classList.add('show')}
    function updateStake(){var amount=betInput?Number(betInput.value||0):0;if(betUsd)betUsd.textContent='≈ '+fmtUsd(amount*(tonUsdPrice||0));if(betEstimate){var pools=currentRound&&currentRound.pools,side=currentBetSide==='down'?'down':'up',other=side==='up'?'down':'up',p=0;if(amount&&pools){var sp=Number(pools[side]&&pools[side].stakeTon||0),op=Number(pools[other]&&pools[other].stakeTon||0);p=op<=0?amount:amount+(amount/(sp+amount))*op*.95}betEstimate.textContent=amount>0?'+'+fmtTon(p):''}}
    function openBet(side){currentBetSide=side==='down'?'down':'up';if(betTitle)betTitle.textContent=(currentBetSide==='down'?'Down':'Up')+' on '+market().label;if(betQuestion)betQuestion.textContent=market().question;if(betInput)betInput.value='';setStatus('','');updateStake();connectTon();sheet.classList.add('open');sheet.setAttribute('aria-hidden','false');setTimeout(function(){try{betInput&&betInput.focus()}catch(e){}},160)}function closeBet(){sheet.classList.remove('open');sheet.setAttribute('aria-hidden','true')}
    function placeBet(){if(betBusy)return;var amount=betInput?Number(betInput.value||0):0;if(!uid()){setStatus('Telegram user was not detected.','bad');return}if(!amount||amount<=0){setStatus('Enter a valid TON amount.','bad');return}betBusy=true;if(betSubmit){betSubmit.disabled=true;betSubmit.textContent='Placing...'}setStatus('Checking balance...','');fetch('/app/api/predict-bet',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId:uid(),market:activeMarket,side:currentBetSide,stakeTon:amount,tonUsdSnapshot:tonUsdPrice||0})}).then(function(r){return r.json().then(function(j){return{ok:r.ok,json:j}})}).then(function(res){if(!res.ok||!res.json||res.json.ok===false)throw new Error((res.json&&res.json.error)||'Could not place prediction');setStatus('Prediction placed.','good');syncBalance(res.json);if(res.json.round&&res.json.round.round){currentRound=res.json.round.round;renderHistory(currentRound)}syncRound(true);setTimeout(closeBet,450)}).catch(function(e){setStatus(e&&e.message?e.message:'Could not place prediction.','bad')}).finally(function(){betBusy=false;if(betSubmit){betSubmit.disabled=false;betSubmit.textContent='Place prediction'}})}
    function spawnBet(){if(!liveBetBox||!active())return;var nums=['+23','+62','+18','+41','+27','+76','+34','+12'],sides=['up','down'];var item=document.createElement('span');item.className='predict-zone-live-bet '+sides[Math.floor(Math.random()*2)];item.textContent=nums[Math.floor(Math.random()*nums.length)];item.style.left=(Math.floor(Math.random()*18))+'px';liveBetBox.appendChild(item);setTimeout(function(){try{item.remove()}catch(e){}},2600)}function startLiveBets(){if(liveBetTimer)return;spawnBet();liveBetTimer=setInterval(spawnBet,2500)}function stopLiveBets(){if(liveBetTimer){clearInterval(liveBetTimer);liveBetTimer=0}}
    function setMarket(key){if(!markets[key])return;if(activeMarket===key&&realReady)return;activeMarket=key;menu.querySelectorAll('[data-vexa-predict-market]').forEach(function(b){b.classList.toggle('active',b.getAttribute('data-vexa-predict-market')===key)});if(question)question.textContent=market().question;applyMarketImage();if(betQuestion)betQuestion.textContent=market().question;if(start)start.textContent='Loading';if(live)live.textContent='Loading';if(countdown)countdown.textContent='--:--';if(resultBox){resultBox.className='predict-zone-result-strip';resultBox.innerHTML=''}if(liveBetBox)liveBetBox.innerHTML='';roundStartPrice=0;currentRound=null;historyKey='';roundStartedAt=Math.floor(Date.now()/300000)*300000;clearChart();stop();startEngine()}
    menu.addEventListener('click',function(ev){var btn=ev.target.closest&&ev.target.closest('[data-vexa-predict-market]');if(!btn)return;var key=btn.getAttribute('data-vexa-predict-market');if(!markets[key])return;ev.preventDefault();ev.stopPropagation();setMarket(key)},true);
    card.querySelectorAll('[data-predict-choice]').forEach(function(b){b.addEventListener('click',function(){openBet(b.getAttribute('data-predict-choice'))})});
    sheet.querySelectorAll('[data-predict-bet-preset]').forEach(function(b){b.addEventListener('click',function(){if(betInput){betInput.value=b.getAttribute('data-predict-bet-preset')||'';updateStake()}})});
    if(betInput)betInput.addEventListener('input',updateStake);if(betClose)betClose.addEventListener('click',closeBet);sheet.addEventListener('click',function(e){if(e.target===sheet)closeBet()});if(betSubmit)betSubmit.addEventListener('click',placeBet);
    document.addEventListener('visibilitychange',function(){if(document.visibilityState==='visible'){syncRound(true);startEngine()}else stop()});document.addEventListener('click',function(){setTimeout(startEngine,70)},true);if(window.MutationObserver)new MutationObserver(function(){if(active())startEngine();else stop()}).observe(root,{attributes:true,attributeFilter:['class']});
    loadMarketImages();
    setMarket('bitcoin');
  });
})();
`;