export const WHEEL_SECTION = `<section id="wheel" class="view wheel-view"><style>html:has(#wheel.active),body:has(#wheel.active){background:#000!important;background-image:none!important}body:has(#wheel.active) .app,body:has(#wheel.active) main.app{padding:0!important;background:#000!important;background-image:none!important;box-shadow:none!important}body:has(#wheel.active) .content{height:100dvh!important;margin:0!important;width:100%!important;overflow:hidden!important;background:#000!important;background-image:none!important}body:has(#wheel.active) .top,body:has(#wheel.active) header.top{display:none!important;height:0!important;min-height:0!important;padding:0!important;margin:0!important;overflow:hidden!important;background:#000!important;background-image:none!important;box-shadow:none!important}body:has(#wheel.active) .tabs{display:none!important}.wheel-view{position:relative;height:100dvh;min-height:100dvh;padding:0 14px calc(96px + env(safe-area-inset-bottom));background:#000!important;background-image:none!important;color:white;overflow-y:auto!important;overflow-x:hidden;-webkit-overflow-scrolling:touch}.wheel-wrap{position:relative;z-index:1;max-width:520px;margin:0 auto;display:grid;gap:14px}.wheel-stage{position:relative;height:398px;display:grid;place-items:center;margin-top:8px}.wheel-canvas{position:relative;z-index:1;width:min(376px,91vw);height:min(376px,91vw);filter:none}.wheel-pointer{position:absolute;top:6px;left:50%;width:44px;height:52px;z-index:5;transform:translateX(-50%);background:transparent!important}.wheel-pointer:before{content:'';position:absolute;inset:0;background:linear-gradient(180deg,#6b102e,#320612);box-shadow:0 8px 16px rgba(0,0,0,.5);clip-path:path('M22 52 C20 52 18 50 16 47 L3 18 C1 13 3 9 8 7 L17 2 C20 0 24 0 27 2 L36 7 C41 9 43 13 41 18 L28 47 C26 50 24 52 22 52 Z')}.wheel-center{position:absolute;width:66px;height:66px;border-radius:50%;display:grid;place-items:center;background:rgba(255,255,255,.075)!important;border:1px solid rgba(255,255,255,.32)!important;box-shadow:0 18px 36px rgba(0,0,0,.62),inset 0 1px 0 rgba(255,255,255,.42)!important;backdrop-filter:blur(6px);-webkit-backdrop-filter:blur(6px);z-index:4;text-align:center}.wheel-center b{font-size:11px;color:#fff;letter-spacing:-.04em}.wheel-panel{position:relative;border:0!important;border-radius:28px;background:transparent!important;box-shadow:none!important;padding:14px;margin-bottom:48px}.wheel-controls{display:grid;gap:10px}.wheel-input-row{display:grid;grid-template-columns:1fr auto;gap:8px}.wheel-input-row input{height:50px;border-radius:18px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.06);color:#fff;padding:0 14px;font-size:18px;font-weight:900;outline:none}.wheel-input-row span{height:50px;min-width:64px;border-radius:18px;border:1px solid rgba(255,255,255,.1);background:rgba(0,0,0,.35);display:grid;place-items:center;font-weight:950}.wheel-quick{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.wheel-quick button,.wheel-join{border:1px solid rgba(255,255,255,.12);border-radius:18px;background:rgba(255,255,255,.06);color:#fff;font-weight:900;height:44px;box-shadow:inset 0 1px 0 rgba(255,255,255,.08)}.wheel-quick button.active{background:#4a0a1e;border-color:#5f0d27}.wheel-join{height:60px;border-radius:999px;font-size:18px;background:#fff;color:#050506;letter-spacing:-.045em;border-color:#fff;box-shadow:0 14px 28px rgba(0,0,0,.58)}.wheel-join:disabled{opacity:.58}.wheel-status{min-height:20px;color:rgba(255,255,255,.7);font-size:12px;text-align:center;font-weight:800}.wheel-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px}.wheel-stat{border:1px solid rgba(255,255,255,.1);border-radius:18px;background:rgba(0,0,0,.35);padding:11px;text-align:center}.wheel-stat small{display:block;color:rgba(255,255,255,.45);font-size:10px;font-weight:850}.wheel-stat b{display:block;margin-top:4px;font-size:14px}.wheel-players{display:grid;gap:8px;margin-top:12px}.wheel-player{display:grid;grid-template-columns:1fr auto;gap:10px;align-items:center;border:1px solid rgba(255,255,255,.09);border-radius:16px;background:rgba(255,255,255,.045);padding:10px 12px}.wheel-player strong{font-size:13px}.wheel-player small{display:block;color:rgba(255,255,255,.48);font-size:10px}.wheel-player b{font-size:13px}.wheel-winner{border-color:rgba(255,255,255,.36);background:rgba(255,255,255,.12)}@media(max-width:420px){.wheel-stage{height:376px}.wheel-canvas{width:min(356px,92vw);height:min(356px,92vw)}.wheel-center{width:60px;height:60px}}</style><div class="wheel-wrap"><div class="wheel-stage"><div class="wheel-pointer"></div><canvas class="wheel-canvas" width="1200" height="1200" data-wheel-canvas></canvas><div class="wheel-center"><b>WHEEL</b></div></div><div class="wheel-panel"><div class="wheel-controls"><div class="wheel-input-row"><input data-wheel-amount inputmode="decimal" pattern="[0-9.]*" value="0.01"/><span>TON</span></div><div class="wheel-quick"><button data-wheel-quick="0.01" class="active">0.01</button><button data-wheel-quick="0.05">0.05</button><button data-wheel-quick="0.1">0.1</button></div><button class="wheel-join" data-wheel-join>Join Round</button><div class="wheel-status" data-wheel-status>Loading round...</div></div><div class="wheel-stats"><div class="wheel-stat"><small>PLAYERS</small><b data-wheel-count>0/5</b></div><div class="wheel-stat"><small>POT</small><b data-wheel-pot>0 TON</b></div><div class="wheel-stat"><small>YOUR BET</small><b data-wheel-user>0 TON</b></div></div><div class="wheel-players" data-wheel-players></div></div></div><script>(function(){var root=document.getElementById('wheel');if(!root||root.dataset.readyWeightedTon)return;root.dataset.readyWeightedTon='1';var tg=window.Telegram&&window.Telegram.WebApp;var user=(tg&&tg.initDataUnsafe&&tg.initDataUnsafe.user)||{};var ownerId=localStorage.getItem('ownerId')||String(user.id||'');var username=String(user.username||'');var firstName=String(user.first_name||'');var canvas=root.querySelector('[data-wheel-canvas]'),ctx=canvas.getContext('2d'),amountInput=root.querySelector('[data-wheel-amount]'),joinBtn=root.querySelector('[data-wheel-join]'),statusEl=root.querySelector('[data-wheel-status]'),countEl=root.querySelector('[data-wheel-count]'),potEl=root.querySelector('[data-wheel-pot]'),userEl=root.querySelector('[data-wheel-user]'),playersEl=root.querySelector('[data-wheel-players]');var NANO=1000000000,angle=-Math.PI/2,state=null,spinning=false,lastClosedRound='';var colors=['#f2f2f2','#4a0a1e','#19191c','#343438','#101012'];function ton(n){return (Math.max(0,Math.floor(Number(n)||0))/NANO).toFixed(4).replace(/\\.0+$/,'').replace(/(\\.\\d*?)0+$/,'$1')}function amountNano(){var n=Number(String(amountInput.value||'').replace(',','.'));if(!isFinite(n)||n<.01)n=.01;amountInput.value=String(n);return Math.floor(n*NANO)}function toast(v){var n=document.getElementById('toast');if(!n)return;n.textContent=v;n.style.display='block';setTimeout(function(){n.style.display='none'},2500)}function label(e){return e.firstName||e.username||('User '+String(e.userId||'').slice(-4))}function segments(){var entries=state&&state.entries&&state.entries.length?state.entries:[{firstName:'Waiting',username:'',userId:'',amountNano:1,amountTon:'0',ticketStart:1,ticketEnd:1}];var total=(state&&state.round&&state.round.totalAmountNano)||entries.reduce(function(s,e){return s+Number(e.amountNano||0)},0)||entries.length;var out=[],cursor=0;entries.forEach(function(e,i){var share=Math.max(0,Number(e.amountNano||0))/total;if(!share)share=1/entries.length;out.push({entry:e,start:cursor,end:cursor+share,color:colors[i%colors.length]});cursor+=share});return out}function draw(){var w=1200,h=1200,cx=600,cy=600,r=486,ri=106,gap=.008;ctx.clearRect(0,0,w,h);ctx.beginPath();ctx.arc(cx,cy,r+42,0,Math.PI*2);ctx.fillStyle='#030304';ctx.fill();segments().forEach(function(s){var a0=angle+s.start*Math.PI*2+gap,a1=angle+s.end*Math.PI*2-gap,am=(a0+a1)/2;ctx.beginPath();ctx.moveTo(cx+Math.cos(a0)*ri,cy+Math.sin(a0)*ri);ctx.arc(cx,cy,r,a0,a1);ctx.lineTo(cx+Math.cos(a1)*ri,cy+Math.sin(a1)*ri);ctx.arc(cx,cy,ri,a1,a0,true);ctx.closePath();ctx.fillStyle=s.color;ctx.globalAlpha=.72;ctx.fill();ctx.globalAlpha=1;ctx.save();ctx.translate(cx,cy);ctx.rotate(am);ctx.fillStyle=s.color==='#f2f2f2'?'#080809':'#fff';ctx.textAlign='center';ctx.textBaseline='middle';ctx.font='900 46px system-ui';ctx.fillText(ton(s.entry.amountNano),r*.58,0);ctx.restore()});ctx.beginPath();ctx.arc(cx,cy,r+38,0,Math.PI*2);ctx.strokeStyle='rgba(55,4,22,.86)';ctx.lineWidth=12;ctx.stroke();ctx.beginPath();ctx.arc(cx,cy,r+33,0,Math.PI*2);ctx.strokeStyle='rgba(255,255,255,.18)';ctx.lineWidth=2;ctx.stroke()}function render(data){state=data;var entries=data.entries||[],round=data.round||{};var mine=entries.find(function(e){return String(e.userId)===String(ownerId)});countEl.textContent=entries.length+'/'+(data.maxPlayers||5);potEl.textContent=(round.totalTon||'0')+' TON';userEl.textContent=mine?(mine.amountTon+' TON'):'0 TON';joinBtn.disabled=spinning||round.status==='closed'||!!mine||entries.length>=(data.maxPlayers||5)||!ownerId;joinBtn.textContent=mine?'Joined':round.status==='closed'?'Round Closed':'Join Round';statusEl.textContent=round.status==='closed'&&round.winner?('Winner: '+label(round.winner)+' · '+round.totalTon+' TON'):(entries.length?(entries.length+' players joined. Round closes at 5 players.'):'Waiting for players.');playersEl.innerHTML=entries.map(function(e){var won=round.winnerUserId&&round.winnerUserId===e.userId;return '<div class="wheel-player '+(won?'wheel-winner':'')+'"><span><strong>'+label(e).replace(/[<>]/g,'')+'</strong><small>'+Math.round((Number(e.amountNano||0)/Math.max(1,Number(round.totalAmountNano||0)))*100)+'% chance</small></span><b>'+e.amountTon+' TON</b></div>'}).join('');draw();if(round.status==='closed'&&round.selectedTicket&&round.id!==lastClosedRound)animateToWinner(round)}function animateToWinner(round){lastClosedRound=round.id;spinning=true;joinBtn.disabled=true;var total=Math.max(1,Number(round.totalAmountNano||0));var selected=(Math.max(1,Number(round.selectedTicket||1))-.5)/total;var pointer=-Math.PI/2;var start=angle;var end=pointer-selected*Math.PI*2-Math.PI*2*7;var t0=performance.now(),dur=4300;function ease(t){return 1-Math.pow(1-t,5)}function step(now){var t=Math.min(1,(now-t0)/dur);angle=start+(end-start)*ease(t);draw();if(t<1)requestAnimationFrame(step);else{angle=end;draw();spinning=false;setTimeout(loadRound,1400)}}requestAnimationFrame(step)}async function api(path,opt){var r=await fetch(path,Object.assign({headers:{'content-type':'application/json','accept':'application/json'}},opt||{}));var j=await r.json().catch(function(){return{error:'Invalid response'}});if(!r.ok||j.error)throw new Error(j.error||'Request failed');return j}async function loadRound(){try{render(await api('/app/api/wheel-round',{cache:'no-store'}))}catch(e){statusEl.textContent=e.message;draw()}}async function join(){if(!ownerId)return toast('Telegram user not found');try{joinBtn.disabled=true;statusEl.textContent='Joining round...';var data=await api('/app/api/wheel-round/join',{method:'POST',body:JSON.stringify({userId:ownerId,username:username,firstName:firstName,amountNano:amountNano()})});if(window.VexaTonBalance&&window.VexaTonBalance.load)window.VexaTonBalance.load();render(data)}catch(e){statusEl.textContent=e.message;toast(e.message);joinBtn.disabled=false}}root.querySelectorAll('[data-wheel-quick]').forEach(function(b){b.onclick=function(){root.querySelectorAll('[data-wheel-quick]').forEach(function(x){x.classList.remove('active')});b.classList.add('active');amountInput.value=b.getAttribute('data-wheel-quick')||'0.01'}});joinBtn.onclick=join;draw();loadRound();setInterval(function(){var active=document.querySelector('.view.active');if(active&&active.id==='wheel'&&!spinning)loadRound()},5000)})();</script></section>`;