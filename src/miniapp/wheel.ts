export const WHEEL_SECTION = `<section id="wheel" class="view wheel-view"><style>html:has(#wheel.active),body:has(#wheel.active){background:#000!important;background-color:#000!important;background-image:none!important}body:has(#wheel.active)::before,body:has(#wheel.active)::after,body:has(#wheel.active) .app::before,body:has(#wheel.active) .app::after,body:has(#wheel.active) .content::before,body:has(#wheel.active) .content::after,body:has(#wheel.active) #wheel::before,body:has(#wheel.active) #wheel::after{background:#000!important;background-color:#000!important;background-image:none!important;box-shadow:none!important}body:has(#wheel.active) .app,body:has(#wheel.active) main.app,body:has(#wheel.active) .content,body:has(#wheel.active) .view.active,body:has(#wheel.active) #wheel,body:has(#wheel.active) .wheel-view,body:has(#wheel.active) .top,body:has(#wheel.active) header.top{background:#000!important;background-color:#000!important;background-image:none!important;box-shadow:none!important}body:has(#wheel.active) .tabs{display:none!important}.wheel-view{position:relative;box-sizing:border-box;height:100%;min-height:100%;padding:0 14px calc(96px + env(safe-area-inset-bottom));background:#000!important;background-color:#000!important;background-image:none!important;color:white;overflow-y:auto!important;overflow-x:hidden;-webkit-overflow-scrolling:touch}.wheel-wrap{position:relative;z-index:1;max-width:520px;margin:0 auto;display:grid;gap:14px}.wheel-stage{position:relative;height:398px;display:grid;place-items:center;margin-top:8px}.wheel-canvas{position:relative;z-index:1;width:min(376px,91vw);height:min(376px,91vw);filter:none}.wheel-pointer{position:absolute;top:-3px;left:50%;width:38px;height:66px;z-index:5;transform:translateX(-50%);background:transparent!important;filter:drop-shadow(0 14px 18px rgba(0,0,0,.58))}.wheel-pointer:before{content:'';position:absolute;left:50%;top:0;width:16px;height:26px;border-radius:999px;background:linear-gradient(180deg,rgba(255,255,255,.20),rgba(255,255,255,.03)),linear-gradient(180deg,#8b173e,#340611);box-shadow:inset 0 1px 0 rgba(255,255,255,.32),0 8px 18px rgba(0,0,0,.46);transform:translateX(-50%)}.wheel-pointer:after{content:'';position:absolute;left:50%;top:18px;width:38px;height:48px;transform:translateX(-50%);background:linear-gradient(180deg,rgba(255,255,255,.18),rgba(255,255,255,0) 35%),linear-gradient(180deg,#8a173a 0%,#4d0a20 46%,#170308 100%);box-shadow:inset 0 1px 0 rgba(255,255,255,.22),inset 0 -1px 0 rgba(0,0,0,.45);clip-path:polygon(50% 100%,41% 80%,4% 20%,25% 8%,50% 0,75% 8%,96% 20%,59% 80%)}.wheel-center{position:absolute;width:60px;height:60px;border-radius:50%;display:grid;place-items:center;background:rgba(255,255,255,.06)!important;border:1px solid rgba(255,255,255,.24)!important;box-shadow:0 16px 32px rgba(0,0,0,.62),inset 0 1px 0 rgba(255,255,255,.32)!important;backdrop-filter:blur(6px);-webkit-backdrop-filter:blur(6px);z-index:4;text-align:center}.wheel-center b{font-size:10px;color:rgba(255,255,255,.62);letter-spacing:-.04em;text-shadow:0 2px 8px rgba(0,0,0,.75)}.wheel-panel{position:relative;border:0!important;border-radius:28px;background:transparent!important;box-shadow:none!important;padding:14px;margin-bottom:48px}.wheel-controls{display:grid;gap:10px}.wheel-input-row{display:grid;grid-template-columns:1fr auto;gap:8px}.wheel-input-row input{height:50px;border-radius:18px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.06);color:#fff;padding:0 14px;font-size:18px;font-weight:900;outline:none}.wheel-input-row span{height:50px;min-width:64px;border-radius:18px;border:1px solid rgba(255,255,255,.1);background:rgba(0,0,0,.35);display:grid;place-items:center;font-weight:950}.wheel-quick{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.wheel-quick button,.wheel-join{border:1px solid rgba(255,255,255,.12);border-radius:18px;background:rgba(255,255,255,.06);color:#fff;font-weight:900;height:44px;box-shadow:inset 0 1px 0 rgba(255,255,255,.08)}.wheel-quick button.active{background:#4a0a1e;border-color:#5f0d27}.wheel-join{height:60px;border-radius:999px;font-size:18px;background:#fff;color:#050506;letter-spacing:-.045em;border-color:#fff;box-shadow:0 14px 28px rgba(0,0,0,.58)}.wheel-join:disabled{opacity:.58}.wheel-status{min-height:20px;color:rgba(255,255,255,.7);font-size:12px;text-align:center;font-weight:800}.wheel-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px}.wheel-stat{border:1px solid rgba(255,255,255,.1);border-radius:18px;background:rgba(0,0,0,.35);padding:11px;text-align:center}.wheel-stat small{display:block;color:rgba(255,255,255,.45);font-size:10px;font-weight:850}.wheel-stat b{display:block;margin-top:4px;font-size:14px}.wheel-players{display:grid;gap:8px;margin-top:12px}.wheel-player{display:grid;grid-template-columns:1fr auto;gap:10px;align-items:center;border:1px solid rgba(255,255,255,.09);border-radius:16px;background:rgba(255,255,255,.045);padding:10px 12px;animation:wheelPlayerIn .36s cubic-bezier(.2,.8,.2,1);transition:border-color .22s ease,background .22s ease,transform .22s ease}.wheel-player strong{font-size:13px}.wheel-player small{display:block;color:rgba(255,255,255,.48);font-size:10px}.wheel-player b{font-size:13px}.wheel-winner{border-color:rgba(255,255,255,.36);background:rgba(255,255,255,.12)}@keyframes wheelPlayerIn{from{opacity:0;transform:translateY(8px) scale(.98)}to{opacity:1;transform:translateY(0) scale(1)}}@media(max-width:420px){.wheel-stage{height:376px}.wheel-canvas{width:min(356px,92vw);height:min(356px,92vw)}.wheel-center{width:56px;height:56px}}</style><div class="wheel-wrap"><div class="wheel-stage"><div class="wheel-pointer"></div><canvas class="wheel-canvas" width="1200" height="1200" data-wheel-canvas></canvas><div class="wheel-center"><b>WHEEL</b></div></div><div class="wheel-panel"><div class="wheel-controls"><div class="wheel-input-row"><input data-wheel-amount inputmode="decimal" pattern="[0-9.]*" value="0.01"/><span>TON</span></div><div class="wheel-quick"><button data-wheel-quick="0.01" class="active">0.01</button><button data-wheel-quick="0.05">0.05</button><button data-wheel-quick="0.1">0.1</button></div><button class="wheel-join" data-wheel-join>Join Round</button><div class="wheel-status" data-wheel-status>Loading round...</div></div><div class="wheel-stats"><div class="wheel-stat"><small>PLAYERS</small><b data-wheel-count>0/5</b></div><div class="wheel-stat"><small>POT</small><b data-wheel-pot>0 TON</b></div><div class="wheel-stat"><small>YOUR BET</small><b data-wheel-user>0 TON</b></div></div><div class="wheel-players" data-wheel-players></div></div></div><script>(function(){var root=document.getElementById('wheel');if(!root||root.dataset.readyWeightedTon)return;root.dataset.readyWeightedTon='1';var tg=window.Telegram&&window.Telegram.WebApp;var user=(tg&&tg.initDataUnsafe&&tg.initDataUnsafe.user)||{};var ownerId=localStorage.getItem('ownerId')||String(user.id||'');var username=String(user.username||'');var firstName=String(user.first_name||'');var canvas=root.querySelector('[data-wheel-canvas]'),ctx=canvas.getContext('2d'),amountInput=root.querySelector('[data-wheel-amount]'),joinBtn=root.querySelector('[data-wheel-join]'),statusEl=root.querySelector('[data-wheel-status]'),countEl=root.querySelector('[data-wheel-count]'),potEl=root.querySelector('[data-wheel-pot]'),userEl=root.querySelector('[data-wheel-user]'),playersEl=root.querySelector('[data-wheel-players]');var NANO=1000000000,angle=-Math.PI/2,state=null,spinning=false,lastClosedRound='',lastEntryKey='',sliceFrame=0,spinTimer=0,motionFrame=0,timer=0;var displayEntries=[],newSliceIds={},newSliceStart=0,newSliceMs=1250;var colors=['#f2f2f2','#4a0a1e','#19191c','#343438','#101012'];function ton(n){return (Math.max(0,Math.floor(Number(n)||0))/NANO).toFixed(4).replace(/\\.0+$/,'').replace(/(\\.\\d*?)0+$/,'$1')}function amountNano(){var n=Number(String(amountInput.value||'').replace(',','.'));if(!isFinite(n)||n<.01)n=.01;amountInput.value=String(n);return Math.floor(n*NANO)}function toast(v){var n=document.getElementById('toast');if(!n)return;n.textContent=v;n.style.display='block';setTimeout(function(){n.style.display='none'},2500)}function label(e){return e.firstName||e.username||('User '+String(e.userId||'').slice(-4))}function entryKey(entries){return entries.map(function(e){return String(e.userId)+':'+String(e.amountNano)}).join('|')}function cloneEntries(entries){return entries.map(function(e){return {id:e.id,roundId:e.roundId,userId:e.userId,username:e.username,firstName:e.firstName,amountNano:Number(e.amountNano||0),amountTon:e.amountTon,ticketStart:e.ticketStart,ticketEnd:e.ticketEnd,createdAt:e.createdAt}})}function ease(t){return 1-Math.pow(1-t,4)}function markNewSlices(ids){if(!ids.length)return;newSliceIds={};ids.forEach(function(id){newSliceIds[String(id)]=true});newSliceStart=performance.now();if(motionFrame)cancelAnimationFrame(motionFrame);function loop(){draw();if(performance.now()-newSliceStart<newSliceMs)motionFrame=requestAnimationFrame(loop);else{newSliceIds={};motionFrame=0;draw()}}motionFrame=requestAnimationFrame(loop)}function animateSlices(entries,done){var target=cloneEntries(entries);if(sliceFrame)cancelAnimationFrame(sliceFrame);if(!displayEntries.length){displayEntries=target;draw();if(done)done();return}var start=cloneEntries(displayEntries),oldIds=new Set(start.map(function(e){return String(e.userId)})),added=target.filter(function(e){return !oldIds.has(String(e.userId))}).map(function(e){return e.userId});markNewSlices(added);var t0=performance.now(),dur=1050;function amountAt(id){var item=start.find(function(e){return String(e.userId)===String(id)});return item?Number(item.amountNano||0):0}function frame(now){var p=Math.min(1,(now-t0)/dur),ep=ease(p);displayEntries=target.map(function(e){var a=amountAt(e.userId),b=Number(e.amountNano||0);return Object.assign({},e,{amountNano:a+(b-a)*ep})});draw();if(p<1)sliceFrame=requestAnimationFrame(frame);else{displayEntries=target;draw();if(done)done()}}sliceFrame=requestAnimationFrame(frame)}function segments(){var entries=displayEntries.length?displayEntries:(state&&state.entries&&state.entries.length?cloneEntries(state.entries):[{firstName:'Waiting',username:'',userId:'',amountNano:1,amountTon:'0',ticketStart:1,ticketEnd:1}]);var total=entries.reduce(function(s,e){return s+Math.max(0,Number(e.amountNano||0))},0)||entries.length;var out=[],cursor=0;entries.forEach(function(e,i){var share=Math.max(0,Number(e.amountNano||0))/total;if(!share)share=1/entries.length;out.push({entry:e,start:cursor,end:cursor+share,color:colors[i%colors.length]});cursor+=share});return out}function slicePath(cx,cy,ri,r,a0,a1){var span=Math.max(.001,a1-a0),c=Math.min(.052,span*.24),mid=(ri+r)/2,ia0=a0+c,ia1=a1-c,oa0=a0+c,oa1=a1-c;ctx.beginPath();ctx.moveTo(cx+Math.cos(ia0)*ri,cy+Math.sin(ia0)*ri);ctx.arc(cx,cy,ri,ia0,ia1,false);ctx.quadraticCurveTo(cx+Math.cos(a1)*ri,cy+Math.sin(a1)*ri,cx+Math.cos(a1)*mid,cy+Math.sin(a1)*mid);ctx.quadraticCurveTo(cx+Math.cos(a1)*r,cy+Math.sin(a1)*r,cx+Math.cos(oa1)*r,cy+Math.sin(oa1)*r);ctx.arc(cx,cy,r,oa1,oa0,true);ctx.quadraticCurveTo(cx+Math.cos(a0)*r,cy+Math.sin(a0)*r,cx+Math.cos(a0)*mid,cy+Math.sin(a0)*mid);ctx.quadraticCurveTo(cx+Math.cos(a0)*ri,cy+Math.sin(a0)*ri,cx+Math.cos(ia0)*ri,cy+Math.sin(ia0)*ri);ctx.closePath()}function draw(){var w=1200,h=1200,cx=600,cy=600,r=486,ri=106,gap=.014,now=performance.now();ctx.clearRect(0,0,w,h);ctx.beginPath();ctx.arc(cx,cy,r+42,0,Math.PI*2);ctx.fillStyle='#030304';ctx.fill();segments().forEach(function(s){var a0=angle+s.start*Math.PI*2+gap,a1=angle+s.end*Math.PI*2-gap,am=(a0+a1)/2,active=newSliceIds[String(s.entry.userId)],p=active?Math.min(1,(now-newSliceStart)/newSliceMs):0,pop=Math.sin(p*Math.PI);slicePath(cx,cy,ri,r,a0,a1);ctx.fillStyle=s.color;ctx.globalAlpha=.72;ctx.fill();ctx.globalAlpha=1;if(active){ctx.save();ctx.shadowColor='rgba(255,255,255,.72)';ctx.shadowBlur=22+32*pop;slicePath(cx,cy,ri,r+14*pop,a0,a1);ctx.fillStyle='rgba(255,255,255,'+((1-p)*.36)+')';ctx.fill();ctx.beginPath();ctx.arc(cx,cy,r+20+18*pop,a0,a1);ctx.strokeStyle='rgba(255,255,255,'+((1-p)*.62)+')';ctx.lineWidth=16;ctx.lineCap='round';ctx.stroke();ctx.restore()}ctx.save();ctx.translate(cx,cy);ctx.rotate(am);ctx.fillStyle=s.color==='#f2f2f2'?'#080809':'#fff';ctx.textAlign='center';ctx.textBaseline='middle';ctx.font='900 46px system-ui';ctx.fillText(ton(s.entry.amountNano),r*.58,0);ctx.restore()});ctx.beginPath();ctx.arc(cx,cy,r+38,0,Math.PI*2);ctx.strokeStyle='rgba(55,4,22,.86)';ctx.lineWidth=12;ctx.stroke();ctx.beginPath();ctx.arc(cx,cy,r+33,0,Math.PI*2);ctx.strokeStyle='rgba(255,255,255,.18)';ctx.lineWidth=2;ctx.stroke()}function scheduleSpin(round,delay){if(!round||round.status!=='closed'||!round.selectedTicket||round.id===lastClosedRound||spinning)return;if(spinTimer)clearTimeout(spinTimer);spinTimer=setTimeout(function(){animateToWinner(round)},delay||6000)}function plan(data){if(timer)clearTimeout(timer);var round=data&&data.round||{},entries=data&&data.entries||[],max=data&&data.maxPlayers||5;if(!round||round.status!=='open'||entries.length>=max||spinning)return;var started=Date.parse(round.createdAt),wait=3200;if(Number.isFinite(started)){var passed=Date.now()-started;wait=passed<12000?Math.max(800,12000-passed):3200}timer=setTimeout(function(){var active=document.querySelector('.view.active');if(active&&active.id==='wheel'&&!spinning)loadRound()},wait)}function render(data){state=data;var entries=data.entries||[],round=data.round||{};var mine=entries.find(function(e){return String(e.userId)===String(ownerId)}),key=entryKey(entries),changed=key!==lastEntryKey;lastEntryKey=key;countEl.textContent=entries.length+'/'+(data.maxPlayers||5);potEl.textContent=(round.totalTon||'0')+' TON';userEl.textContent=mine?(mine.amountTon+' TON'):'0 TON';joinBtn.disabled=spinning||round.status==='closed'||!!mine||entries.length>=(data.maxPlayers||5)||!ownerId;joinBtn.textContent=mine?'Joined':round.status==='closed'?'Round Closed':'Join Round';statusEl.textContent=round.status==='closed'&&round.winner?('Winner: '+label(round.winner)+' · '+round.totalTon+' TON'):(entries.length?(entries.length+' players joined. Round closes at 5 players.'):'Waiting for players.');playersEl.innerHTML=entries.map(function(e,i){var won=round.winnerUserId&&round.winnerUserId===e.userId;return '<div class="wheel-player '+(won?'wheel-winner':'')+'" style="animation-delay:'+(Math.min(i,4)*42)+'ms"><span><strong>'+label(e).replace(/[<>]/g,'')+'</strong><small>'+Math.round((Number(e.amountNano||0)/Math.max(1,Number(round.totalAmountNano||0)))*100)+'% chance</small></span><b>'+e.amountTon+' TON</b></div>'}).join('');plan(data);if(changed)animateSlices(entries,function(){scheduleSpin(round,6000)});else{if(!displayEntries.length)displayEntries=cloneEntries(entries);draw();scheduleSpin(round,6000)}}function animateToWinner(round){lastClosedRound=round.id;spinning=true;joinBtn.disabled=true;if(spinTimer){clearTimeout(spinTimer);spinTimer=0}if(timer){clearTimeout(timer);timer=0}var total=Math.max(1,Number(round.totalAmountNano||0));var selected=(Math.max(1,Number(round.selectedTicket||1))-.5)/total;var pointer=-Math.PI/2;var start=angle;var end=pointer-selected*Math.PI*2-Math.PI*2*7;var t0=performance.now(),dur=4500;function step(now){var t=Math.min(1,(now-t0)/dur);angle=start+(end-start)*ease(t);draw();if(t<1)requestAnimationFrame(step);else{angle=end;draw();spinning=false;setTimeout(loadRound,3500)}}requestAnimationFrame(step)}async function api(path,opt){var r=await fetch(path,Object.assign({headers:{'content-type':'application/json','accept':'application/json'}},opt||{}));var j=await r.json().catch(function(){return{error:'Invalid response'}});if(!r.ok||j.error)throw new Error(j.error||'Request failed');return j}async function loadRound(){try{render(await api('/app/api/wheel-round',{cache:'no-store'}))}catch(e){statusEl.textContent=e.message;draw()}}async function join(){if(!ownerId)return toast('Telegram user not found');try{joinBtn.disabled=true;statusEl.textContent='Joining round...';var data=await api('/app/api/wheel-round/join',{method:'POST',body:JSON.stringify({userId:ownerId,username:username,firstName:firstName,amountNano:amountNano()})});if(window.VexaTonBalance&&window.VexaTonBalance.load)window.VexaTonBalance.load();render(data)}catch(e){statusEl.textContent=e.message;toast(e.message);joinBtn.disabled=false}}root.querySelectorAll('[data-wheel-quick]').forEach(function(b){b.onclick=function(){root.querySelectorAll('[data-wheel-quick]').forEach(function(x){x.classList.remove('active')});b.classList.add('active');amountInput.value=b.getAttribute('data-wheel-quick')||'0.01'}});joinBtn.onclick=join;draw();loadRound()})();</script></section>`;