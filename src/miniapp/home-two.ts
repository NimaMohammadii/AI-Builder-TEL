// Home Two intentionally starts empty and is mounted instead of Home One when selected by the admin.
export const HOME_TWO_SECTION = `<section id="home" class="view active" data-home-variant="two"></section>`;
