export function miniAppHtml(): string {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>AI Builder TEL</title>
  <script src="https://telegram.org/js/telegram-web-app.js"></script>
  <style>
    :root{--bg:#050505;--card:rgba(255,255,255,.075);--line:rgba(255,255,255,.14);--text:#f8f8f8;--muted:rgba(255,255,255,.62);--white:#fff;--black:#050505;--danger:#fff0f0;--radius:26px;--shadow:0 24px 70px rgba(0,0,0,.48);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color-scheme:dark}
    *{box-sizing:border-box}html,body{margin:0;height:100%;background:var(--bg);color:var(--text);overflow:hidden}body{background:radial-gradient(circle at 86% 8%,rgba(255,255,255,.13),transparent 24%),radial-gradient(circle at 4% 0%,rgba(255,255,255,.06),transparent 20%),linear-gradient(180deg,#101010 0%,#050505 50%,#000 100%)}button,input,textarea,select{font:inherit}button{border:0;cursor:pointer}.app{width:min(100%,560px);height:100dvh;margin:0 auto;padding:calc(14px + env(safe-area-inset-top)) 16px calc(86px + env(safe-area-inset-bottom));position:relative;overflow:hidden}.decor{position:fixed;right:-42px;top:78px;width:220px;height:220px;opacity:.34;pointer-events:none}.topbar{height:56px;display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:8px;position:relative;z-index:2}.brand{display:flex;align-items:center;gap:11px;min-width:0}.mark,.icon{width:42px;height:42px;border-radius:16px;border:1px solid var(--line);background:linear-gradient(145deg,rgba(255,255,255,.15),rgba(255,255,255,.035));display:grid;place-items:center}.icon{border-radius:999px;color:var(--text)}.brand h1{margin:0;font-size:18px;letter-spacing:-.035em}.brand p{margin:2px 0 0;font-size:11px;color:var(--muted)}.content{height:calc(100dvh - 56px - 104px - env(safe-area-inset-top) - env(safe-area-inset-bottom));position:relative;z-index:1;overflow:hidden}.view{display:none;height:100%;overflow-y:auto;overflow-x:hidden;padding:2px 1px 14px;scrollbar-width:none;animation:rise .22s ease both}.view::-webkit-scrollbar,.list::-webkit-scrollbar,.chat::-webkit-scrollbar,.mono::-webkit-scrollbar{display:none}.view.active{display:block}@keyframes rise{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:translateY(0)}}.hero{padding:10px 0 4px}.eyebrow{display:inline-flex;border:1px solid var(--line);border-radius:999px;padding:7px 11px;background:rgba(255,255,255,.06);color:var(--muted);font-size:10.5px;letter-spacing:.09em;text-transform:uppercase}.hero h2{font-size:clamp(34px,10vw,52px);line-height:.94;letter-spacing:-.07em;margin:14px 0 10px;max-width:390px}.hero p{margin:0;max-width:340px;color:var(--muted);font-size:14.5px;line-height:1.42}.actions,.toolbar{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:14px 0}.toolbar{grid-template-columns:repeat(3,1fr);margin-top:12px}.primary,.secondary,.ghost,.danger{min-height:48px;border-radius:17px;display:flex;align-items:center;justify-content:center;gap:9px;font-size:14px;font-weight:760;letter-spacing:-.016em}.primary{color:var(--black);background:var(--white);box-shadow:0 16px 42px rgba(255,255,255,.12)}.secondary,.ghost{color:var(--text);background:rgba(255,255,255,.055);border:1px solid var(--line)}.ghost{min-height:40px;font-size:12px;color:var(--muted)}.danger{min-height:40px;font-size:12px;color:#190202;background:var(--danger)}.grid,.stack{display:grid;gap:13px}.card{border:1px solid var(--line);border-radius:var(--radius);background:linear-gradient(180deg,rgba(255,255,255,.082),rgba(255,255,255,.038));box-shadow:var(--shadow);backdrop-filter:blur(18px);overflow:hidden;position:relative}.card:before{content:"";position:absolute;inset:0;background:linear-gradient(140deg,rgba(255,255,255,.055),transparent 34%);pointer-events:none}.pad{padding:16px;position:relative;z-index:1}.title{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:13px}.title h3{margin:0;font-size:16px;letter-spacing:-.035em}.title span,.tiny{color:var(--muted);font-size:12px;line-height:1.42}.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:9px}.stat{border:1px solid rgba(255,255,255,.09);border-radius:18px;padding:12px;background:rgba(0,0,0,.18)}.stat b{display:block;font-size:22px;letter-spacing:-.05em}.stat span{font-size:11px;color:var(--muted)}.features{display:grid;grid-template-columns:repeat(2,1fr);gap:9px}.feature{min-height:84px;border:1px solid rgba(255,255,255,.09);border-radius:18px;background:rgba(0,0,0,.16);display:flex;align-items:center;gap:11px;padding:11px;color:var(--muted);font-size:12px;line-height:1.18}.round{width:42px;height:42px;border-radius:999px;border:1px solid var(--line);background:rgba(255,255,255,.05);display:grid;place-items:center;flex:0 0 auto}.list{display:grid;gap:9px;overflow-y:auto}.home-bots{max-height:168px}.bot-row{display:grid;grid-template-columns:42px minmax(0,1fr) auto;align-items:center;gap:11px;border:1px solid rgba(255,255,255,.09);border-radius:18px;padding:10px;background:rgba(0,0,0,.22)}.bot-row strong{display:block;font-size:14px;letter-spacing:-.02em;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.bot-row small{display:block;margin-top:2px;color:var(--muted);font-size:11.5px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.pill{border:1px solid var(--line);border-radius:999px;padding:6px 9px;color:var(--muted);font-size:11px;white-space:nowrap}.pill.active{background:rgba(255,255,255,.95);color:#050505}.pill.paused{background:rgba(255,255,255,.08)}.tabs{height:64px;position:absolute;left:16px;right:16px;bottom:calc(12px + env(safe-area-inset-bottom));z-index:5;border:1px solid var(--line);border-radius:24px;background:rgba(8,8,8,.88);backdrop-filter:blur(24px);display:grid;grid-template-columns:repeat(4,1fr);padding:7px;box-shadow:0 16px 42px rgba(0,0,0,.46)}.tab{min-height:48px;border-radius:17px;color:var(--muted);background:transparent;display:grid;place-items:center;gap:2px;font-size:10.5px}.tab.active{color:var(--black);background:var(--white)}.field{display:grid;gap:7px;margin:12px 0}label{font-size:10.5px;color:var(--muted);text-transform:uppercase;letter-spacing:.09em}input,textarea,select{width:100%;border:1px solid var(--line);background:rgba(0,0,0,.28);color:var(--text);border-radius:17px;padding:13px 14px;outline:none;font-size:14px}textarea{min-height:126px;resize:vertical;line-height:1.42}input:focus,textarea:focus,select:focus{border-color:rgba(255,255,255,.42);box-shadow:0 0 0 4px rgba(255,255,255,.055)}.chat{height:258px;overflow-y:auto;display:flex;flex-direction:column;gap:9px;padding:12px;border:1px solid var(--line);border-radius:20px;background:rgba(0,0,0,.24)}.msg{max-width:90%;padding:10px 12px;border-radius:16px;line-height:1.36;font-size:13px;white-space:pre-wrap}.ai{align-self:flex-start;background:rgba(255,255,255,.08);border:1px solid var(--line)}.me{align-self:flex-end;background:var(--white);color:var(--black)}.composer{display:grid;grid-template-columns:minmax(0,1fr) 50px;gap:9px;margin-top:10px}.send{width:50px;height:50px;border-radius:17px;background:var(--white);color:var(--black);display:grid;place-items:center}.mono{font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,monospace;font-size:11px;color:var(--muted);white-space:pre-wrap;max-height:210px;overflow-y:auto}.notice{color:var(--muted);line-height:1.42;font-size:13px;border:1px dashed rgba(255,255,255,.16);border-radius:18px;padding:14px;background:rgba(255,255,255,.035)}.empty{color:var(--muted);font-size:13px;padding:12px 0;line-height:1.42}.toast{position:fixed;left:16px;right:16px;bottom:calc(86px + env(safe-area-inset-bottom));z-index:20;max-width:528px;margin:0 auto;padding:12px 14px;border:1px solid var(--line);border-radius:17px;background:rgba(18,18,18,.96);box-shadow:var(--shadow);display:none;font-size:13px}.skeleton{height:62px;border-radius:18px;background:linear-gradient(90deg,rgba(255,255,255,.04),rgba(255,255,255,.1),rgba(255,255,255,.04));animation:pulse 1.2s infinite}@keyframes pulse{0%,100%{opacity:.5}50%{opacity:1}}@media(max-height:720px){.content{height:calc(100dvh - 56px - 96px - env(safe-area-inset-top) - env(safe-area-inset-bottom))}.decor{width:190px;height:190px;opacity:.3}.hero h2{font-size:clamp(31px,9vw,42px);margin:11px 0 8px}.features{grid-template-columns:repeat(4,1fr);gap:7px}.feature{min-height:74px;padding:8px;display:grid;place-items:center;text-align:center;gap:5px;font-size:10.5px}.round{width:34px;height:34px}.pad{padding:14px}.chat{height:210px}.mono{max-height:128px}}@media(max-width:380px){.actions,.toolbar{grid-template-columns:1fr}.features{grid-template-columns:1fr 1fr}.hero h2{font-size:33px}}
  </style>
</head>
<body>
  <svg class="decor" viewBox="0 0 260 260" fill="none"><circle cx="142" cy="92" r="58" stroke="white" stroke-opacity=".26" stroke-width="1.2"/><circle cx="142" cy="92" r="94" stroke="white" stroke-opacity=".13"/><path d="M30 118h205M142 14v196" stroke="white" stroke-opacity=".08"/><path d="M72 58c43-43 112-47 161-7" stroke="white" stroke-opacity=".31" stroke-width="1.5" stroke-linecap="round"/><path d="M130 93l62-26-23 68-18-27-29-15z" fill="white" fill-opacity=".94"/></svg>
  <main class="app">
    <header class="topbar"><div class="brand"><div class="mark">▦</div><div><h1>AI Builder TEL</h1><p id="userLine">Production workspace</p></div></div><button class="icon" onclick="syncTelegramUser()">↻</button></header>
    <div class="content">
      <section id="home" class="view active"><div class="hero"><span class="eyebrow">Live runtime</span><h2>Build real Telegram bots with AI</h2><p>Connect a token, describe what you want, then manage and improve your bot from one production workspace.</p></div><div class="actions"><button class="primary" onclick="showView('create')">Create Bot</button><button class="secondary" onclick="showView('workspace')">Workspace</button></div><div class="grid"><section class="card"><div class="pad"><div class="title"><h3>Workspace status</h3><span id="statusLine">Syncing</span></div><div class="stats"><div class="stat"><b id="statBots">0</b><span>Total bots</span></div><div class="stat"><b id="statActive">0</b><span>Active</span></div><div class="stat"><b id="statPaused">0</b><span>Paused</span></div></div></div></section><section class="card"><div class="pad"><div class="title"><h3>Capabilities</h3><span>Ready</span></div><div class="features"><div class="feature"><div class="round">↯</div><div>AI generated<br/>executable flow</div></div><div class="feature"><div class="round">●</div><div>Bot token<br/>connection</div></div><div class="feature"><div class="round">▦</div><div>Live bot<br/>management</div></div><div class="feature"><div class="round">✦</div><div>AI change<br/>application</div></div></div></div></section><section class="card"><div class="pad"><div class="title"><h3>Recent bots</h3><button class="ghost" onclick="showView('bots')">Manage</button></div><div id="homeBots" class="list home-bots"><div class="skeleton"></div><div class="skeleton"></div></div></div></section></div></section>
      <section id="create" class="view"><section class="card"><div class="pad"><div class="title"><h3>Create & Publish</h3><span>BotFather token</span></div><div class="field"><label>Bot token</label><input id="token" placeholder="123456789:AA..." autocomplete="off" /></div><div class="field"><label>Bot instructions</label><textarea id="prompt" placeholder="Describe exactly what the bot should do. Example: Ask users for name, phone, and project type. Save the lead and notify me."></textarea></div><button class="primary" id="createBtn" onclick="createBot()">Generate Flow & Publish</button><p class="tiny">The AI creates an executable flow and your bot runs on the central Worker using this token.</p></div></section></section>
      <section id="bots" class="view"><section class="card"><div class="pad"><div class="title"><h3>Bot Management</h3><button class="ghost" onclick="loadBots(true)">Refresh</button></div><div id="botsList" class="list"><div class="skeleton"></div><div class="skeleton"></div><div class="skeleton"></div></div></div></section></section>
      <section id="workspace" class="view"><div class="stack"><section class="card"><div class="pad"><div class="title"><h3>Selected bot</h3><span id="activeBotLabel">No bot selected</span></div><div class="field"><label>Bot</label><select id="botSelect" onchange="selectBot(this.value)"></select></div><div id="botInfo" class="notice">Choose a bot to manage status, webhook, and AI changes.</div><div class="toolbar"><button class="ghost" onclick="publishBot()">Publish</button><button class="ghost" onclick="togglePause()" id="pauseBtn">Pause</button><button class="danger" onclick="deleteBot()">Delete</button></div></div></section><section class="card"><div class="pad"><div class="title"><h3>AI Engineer</h3><span>Applies live</span></div><div id="chat" class="chat"><div class="msg ai">Tell me what to build or change. I update the executable bot flow behind the scenes.</div></div><div class="composer"><input id="chatInput" placeholder="Add a step, collect data, change logic..." onkeydown="if(event.key==='Enter') chatApply()" /><button class="send" onclick="chatApply()">➤</button></div></div></section><section class="card"><div class="pad"><div class="title"><h3>Live flow</h3><span id="flowMeta">JSON</span></div><div id="flowBox" class="mono">No bot selected.</div></div></section></div></section>
      <section id="settings" class="view"><section class="card"><div class="pad"><div class="title"><h3>Settings</h3><span>Account</span></div><div class="field"><label>Telegram User ID</label><input id="ownerId" placeholder="Auto from Telegram Mini App" /></div><button class="primary" onclick="saveOwner()">Save User</button><p class="tiny">Open inside Telegram for automatic user detection. Manual ID is available for browser testing.</p></div></section></section>
    </div>
    <nav class="tabs"><button class="tab active" data-tab="home" onclick="showView('home')">⌂<span>Home</span></button><button class="tab" data-tab="bots" onclick="showView('bots')">◈<span>Bots</span></button><button class="tab" data-tab="workspace" onclick="showView('workspace')">▦<span>Work</span></button><button class="tab" data-tab="settings" onclick="showView('settings')">⚙<span>Settings</span></button></nav>
  </main><div id="toast" class="toast"></div>
  <script>
    const tg=window.Telegram?.WebApp; tg?.ready(); tg?.expand();
    let ownerId=localStorage.getItem('ownerId')||String(tg?.initDataUnsafe?.user?.id||''); let bots=[]; let selectedBot=null; let busy=false;
    const $=id=>document.getElementById(id); $('ownerId').value=ownerId; updateUserLine();
    function updateUserLine(){ $('userLine').textContent=ownerId?'User '+ownerId:'Connect Telegram user'; }
    function syncTelegramUser(){ ownerId=String(tg?.initDataUnsafe?.user?.id||ownerId||''); $('ownerId').value=ownerId; saveOwner(); }
    function saveOwner(){ ownerId=$('ownerId').value.trim(); localStorage.setItem('ownerId',ownerId); updateUserLine(); toast('User saved'); loadBots(true); }
    function showView(id){ document.querySelectorAll('.view').forEach(v=>v.classList.remove('active')); $(id).classList.add('active'); document.querySelectorAll('.tab').forEach(t=>t.classList.toggle('active',t.dataset.tab===id)); if(id==='bots'||id==='workspace'||id==='home') loadBots(); }
    function toast(text){ const el=$('toast'); el.textContent=text; el.style.display='block'; setTimeout(()=>el.style.display='none',2600); }
    async function api(path,options={}){ const r=await fetch(path,{...options,headers:{'content-type':'application/json',...(options.headers||{})}}); const j=await r.json().catch(()=>({error:'Invalid response'})); if(!r.ok) throw new Error(j.error||'Request failed'); return j; }
    function setBusy(v,label='Working...'){ busy=v; if($('createBtn')) $('createBtn').textContent=v?label:'Generate Flow & Publish'; }
    function botRow(bot){ const status=escapeHtml(bot.status); return '<div class="bot-row" onclick="selectBot(\''+bot.id+'\');showView(\'workspace\')"><div class="round">◈</div><div><strong>'+escapeHtml(bot.title)+'</strong><small>'+(bot.username?'@'+escapeHtml(bot.username):bot.id)+'</small></div><span class="pill '+status+'">'+status+'</span></div>'; }
    function emptyBots(){ return '<div class="notice">No bots yet. Create your first bot with a BotFather token and one clear instruction.</div>'; }
    async function loadBots(force=false){ if(!ownerId){ $('botsList').innerHTML='<div class="notice">Open inside Telegram or set your user ID in Settings.</div>'; $('homeBots').innerHTML='<div class="notice">No Telegram user connected.</div>'; return; } if(bots.length&&!force){ renderBots(); return; } try{ const data=await api('/app/api/bots?ownerId='+encodeURIComponent(ownerId)); bots=data.bots||[]; renderBots(); }catch(e){ $('statusLine').textContent='Error'; toast(e.message); } }
    function renderBots(){ const html=bots.length?bots.map(botRow).join(''):emptyBots(); $('botsList').innerHTML=html; $('homeBots').innerHTML=html; $('statBots').textContent=String(bots.length); $('statActive').textContent=String(bots.filter(b=>b.status==='active').length); $('statPaused').textContent=String(bots.filter(b=>b.status==='paused').length); $('statusLine').textContent=bots.length?'Synced':'Ready'; const select=$('botSelect'); select.innerHTML=bots.length?bots.map(b=>'<option value="'+b.id+'">'+escapeHtml(b.title)+'</option>').join(''):'<option value="">No bots</option>'; if(bots[0]&&!selectedBot) selectBot(bots[0].id); }
    async function createBot(){ if(busy) return; const token=$('token').value.trim(); const prompt=$('prompt').value.trim(); if(!ownerId) return toast('Set Telegram user first'); if(!token) return toast('Bot token is required'); if(prompt.length<10) return toast('Describe the bot first'); try{ setBusy(true,'Building...'); toast('Generating and publishing bot...'); const data=await api('/app/api/bots',{method:'POST',body:JSON.stringify({ownerTelegramId:ownerId,telegramToken:token,prompt})}); $('token').value=''; $('prompt').value=''; bots=[]; await loadBots(true); await selectBot(data.botId); showView('workspace'); toast('Published @'+(data.username||'bot')); }catch(e){ toast(e.message); }finally{ setBusy(false); } }
    async function selectBot(id){ if(!id) return; try{ const data=await api('/app/api/bots/'+id); selectedBot=data; $('activeBotLabel').textContent=data.username?'@'+data.username:data.title; $('botInfo').innerHTML='<b>'+escapeHtml(data.title)+'</b><br>Status: '+escapeHtml(data.status)+'<br>Webhook: '+escapeHtml(data.settings?.webhookUrl||'connected'); $('flowBox').textContent=JSON.stringify(data.flow||data.blueprint,null,2); $('flowMeta').textContent=data.flow?'Executable Flow':'Blueprint'; $('pauseBtn').textContent=data.status==='active'?'Pause':'Activate'; const sel=$('botSelect'); if(sel) sel.value=id; }catch(e){ toast(e.message); } }
    function addMsg(text,who){ const el=document.createElement('div'); el.className='msg '+who; el.textContent=text; $('chat').appendChild(el); el.scrollIntoView({behavior:'smooth',block:'end'}); }
    async function chatApply(){ if(!selectedBot) return toast('Select a bot first'); const input=$('chatInput'); const instruction=input.value.trim(); if(!instruction) return; input.value=''; addMsg(instruction,'me'); addMsg('Applying to live flow...','ai'); try{ const data=await api('/app/api/bots/'+selectedBot.id+'/chat',{method:'POST',body:JSON.stringify({instruction})}); document.querySelector('#chat .msg.ai:last-child').textContent=data.summary||'Applied.'; await selectBot(selectedBot.id); }catch(e){ document.querySelector('#chat .msg.ai:last-child').textContent=e.message; } }
    async function publishBot(){ if(!selectedBot) return toast('Select a bot first'); try{ const data=await api('/app/api/bots/'+selectedBot.id+'/publish',{method:'POST'}); toast('Published'); bots=[]; await loadBots(true); await selectBot(data.botId); }catch(e){ toast(e.message); } }
    async function togglePause(){ if(!selectedBot) return toast('Select a bot first'); const status=selectedBot.status==='active'?'paused':'active'; try{ const data=await api('/app/api/bots/'+selectedBot.id+'/status',{method:'PATCH',body:JSON.stringify({status})}); toast(status==='active'?'Activated':'Paused'); bots=[]; await loadBots(true); await selectBot(data.botId); }catch(e){ toast(e.message); } }
    async function deleteBot(){ if(!selectedBot) return toast('Select a bot first'); if(!confirm('Delete this bot?')) return; try{ await api('/app/api/bots/'+selectedBot.id,{method:'DELETE'}); selectedBot=null; bots=[]; await loadBots(true); $('botInfo').textContent='Choose a bot to manage status, webhook, and AI changes.'; $('flowBox').textContent='No bot selected.'; toast('Bot deleted'); }catch(e){ toast(e.message); } }
    function escapeHtml(v){ return String(v||'').replace(/[&<>'"]/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[s])); }
    loadBots(true);
  </script>
</body>
</html>`;
}
