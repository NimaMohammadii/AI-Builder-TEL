export function mobileAdminLoginHtml(message = 'Only admins can open tools.'): string {
  const safeMessage = escapeHtml(message);
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,viewport-fit=cover"/><title>Vexa Admin</title><style>${mobileAdminCss()}</style></head><body><main class="page login"><p class="eyebrow">Vexa FLOW</p><h1>Admin</h1><p class="muted">Enter admin key.</p><form method="post" action="/admin/panel"><label>Admin key</label><input name="key" type="password" placeholder="ADMIN_KEY" autocomplete="current-password" required/><button class="primary" type="submit">Send Telegram code</button></form><p class="status">${safeMessage}</p></main></body></html>`;
}

export function mobileAdminCodeHtml(challengeId: string, message = 'Enter the 6-digit code sent to Telegram.'): string {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,viewport-fit=cover"/><title>Vexa Admin Code</title><style>${mobileAdminCss()}</style></head><body><main class="page login"><p class="eyebrow">Vexa FLOW</p><h1>2FA Code</h1><p class="muted">${escapeHtml(message)}</p><form method="post" action="/admin/verify"><input name="challenge" type="hidden" value="${escapeHtml(challengeId)}"/><label>Telegram code</label><input name="code" type="text" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" placeholder="000000" autocomplete="one-time-code" required/><button class="primary" type="submit">Verify and enter</button></form><p class="status">Code expires in 5 minutes and can be used once.</p></main></body></html>`;
}

function escapeHtml(value: string): string {
  return String(value).replace(/[&<>"]/g, (ch) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[ch] || ch));
}

export function mobileAdminPanelHtml(): string {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,viewport-fit=cover"/><title>Vexa Admin</title><style>${mobileAdminCss()}</style></head><body><main class="page"><header class="head"><div><p class="eyebrow">Vexa FLOW</p><h1 id="adminTitle">Users</h1><p class="muted" id="adminSubtitle">Bot and mini app users.</p></div><div class="head-actions"><button class="icon-menu" id="menuBtn" type="button" aria-label="Open admin menu"><span></span><span></span><span></span></button><button class="ghost" id="logout">Logout</button></div></header><div class="menu-panel" id="adminMenu" hidden><button class="menu-item active" data-section="users" type="button"><strong>Users</strong><span>Credit and per-user access</span></button><button class="menu-item" data-section="locks" type="button"><strong>Section locks</strong><span>Open, locked, access-code and shared lock images</span></button><button class="menu-item" data-section="images" type="button"><strong>Uploaded images</strong><span>Credit icon and uploaded visual assets</span></button><button class="menu-item" id="announcementMenu" data-section="announcement" type="button"><strong>Admin Message</strong><span>Voice and bot delivery</span></button></div><section class="section admin-section active" id="sectionUsers" data-title="Users" data-subtitle="Credit and per-user access."><div class="row-title"><div><h2>Users</h2><p class="muted small-text">Manage credit and section access for each user.</p></div><button class="ghost" id="refreshUsers">Refresh</button></div><div class="stats"><div><span>Total</span><b id="statTotal">0</b></div><div><span>Online</span><b id="statOnline">0</b></div><div><span>Inactive</span><b id="statInactive">0</b></div><div><span>Credit</span><b id="statCredit">0</b></div></div><div class="search"><label for="userSearch">Search users</label><input id="userSearch" type="search" placeholder="ID, username, name, section" autocomplete="off"/><p id="usersStatus" class="status">Loading users...</p></div><div id="usersList" class="users"><div class="empty">Loading...</div></div></section><section class="section admin-section" id="sectionLocks" data-title="Section locks" data-subtitle="Lock sections or require access codes."><div class="row-title"><div><h2>Section locks</h2><p class="muted small-text">Choose Open, Locked, Code lock, and upload one shared image for normal locks and one for code locks.</p></div><button class="ghost" id="refreshLocks">Refresh</button></div><div class="shared-lock-images"><div class="lock-preview" id="sharedLockedPreview"><span>No normal lock image</span></div><div class="lock-image-upload"><input data-shared-lock-image="locked" type="file" accept="image/png,image/jpeg,image/webp,.png,.jpg,.jpeg,.webp"/><button class="lock-toggle" data-upload-shared-lock-image="locked">Upload normal</button></div><div class="lock-preview" id="sharedCodePreview"><span>No code lock image</span></div><div class="lock-image-upload"><input data-shared-lock-image="code" type="file" accept="image/png,image/jpeg,image/webp,.png,.jpg,.jpeg,.webp"/><button class="lock-toggle" data-upload-shared-lock-image="code">Upload code</button></div></div><div id="locksList" class="locks-list"><div class="empty">Loading...</div></div><p id="locksStatus" class="status"></p></section><section class="section admin-section upload" id="sectionImages" data-title="Uploaded images" data-subtitle="Credit icon and uploaded visual assets."><h2>Uploaded images</h2><p class="muted small-text">Manage images used inside the mini app. For now this section contains the credit icon.</p><div class="image-current"><img id="preview" src="/app/api/credit-icon.png" alt=""/><div><strong>Credit icon</strong><p class="muted small-text">Shown next to user credit count.</p></div></div><label>Upload new image</label><input id="file" type="file" accept="image/png,image/jpeg,image/webp,.png,.jpg,.jpeg,.webp"/><button class="primary" id="upload" type="button">Upload icon</button><p id="uploadStatus" class="status"></p></section><section class="section admin-section" id="sectionAnnouncement" data-title="Vexa Admin Message" data-subtitle="Send voice messages to the Mini App or bot chat."></section></main><script>
const file=document.getElementById('file'),statusEl=document.getElementById('uploadStatus'),preview=document.getElementById('preview'),allowed=['image/png','image/jpeg','image/webp'];let adminUsers=[],userQuery='',sectionLocks=[],openUser='';
const menuBtn=document.getElementById('menuBtn'),adminMenu=document.getElementById('adminMenu'),adminTitle=document.getElementById('adminTitle'),adminSubtitle=document.getElementById('adminSubtitle');
const sectionLabels={home:'Home',plinko:'Plinko'};
menuBtn.onclick=()=>{adminMenu.hidden=!adminMenu.hidden};
document.querySelectorAll('.menu-item').forEach(btn=>btn.onclick=()=>{const name=btn.getAttribute('data-section');document.querySelectorAll('.menu-item').forEach(x=>x.classList.toggle('active',x===btn));document.querySelectorAll('.admin-section').forEach(section=>section.classList.toggle('active',section.id==='section'+name.charAt(0).toUpperCase()+name.slice(1)));const active=document.querySelector('.admin-section.active');adminTitle.textContent=active.dataset.title||'Admin';adminSubtitle.textContent=active.dataset.subtitle||'';adminMenu.hidden=true;window.scrollTo({top:0,behavior:'smooth'});if(name==='locks')loadLocks();});
document.getElementById('logout').onclick=async()=>{await fetch('/admin/logout',{method:'POST'}).catch(()=>{});location.href='/admin'};
document.getElementById('upload').onclick=async()=>{if(!file.files||!file.files[0]){statusEl.textContent='Choose an image first.';return}if(!allowed.includes(file.files[0].type)){statusEl.textContent='Only PNG, JPG, JPEG or WebP.';return}statusEl.textContent='Uploading...';const form=new FormData();form.append('icon',file.files[0]);try{const r=await fetch('/admin/upload-credit-icon',{method:'POST',body:form,credentials:'same-origin'});const j=await r.json().catch(()=>({error:'Upload failed'}));if(!r.ok){statusEl.textContent=j.error||'Upload failed';return}preview.src='/app/api/credit-icon.png?t='+Date.now();statusEl.textContent='Uploaded.'}catch(e){statusEl.textContent='Upload failed.'}};
const usersList=document.getElementById('usersList'),usersStatus=document.getElementById('usersStatus'),search=document.getElementById('userSearch'),locksList=document.getElementById('locksList'),locksStatus=document.getElementById('locksStatus');
let locksLoaded=false;
function esc(v){return String(v??'').replace(/[&<>]/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[s]||s))}
function fmt(v){return Number(v||0).toLocaleString('en-US')}
function ago(v){if(!v)return '—';const t=Date.parse(v);if(!t)return v;const d=Math.max(0,Date.now()-t);if(d<60000)return 'now';if(d<3600000)return Math.floor(d/60000)+'m';if(d<86400000)return Math.floor(d/3600000)+'h';return Math.floor(d/86400000)+'d'}
function formatLeft(ms){ms=Math.max(0,Math.floor(Number(ms)||0));const d=Math.floor(ms/86400000),h=Math.floor(ms/3600000)%24,m=Math.floor(ms/60000)%60,sec=Math.floor(ms/1000)%60;return d+'d '+String(h).padStart(2,'0')+':'+String(m).padStart(2,'0')+':'+String(sec).padStart(2,'0')}
function tickCountdowns(){document.querySelectorAll('[data-expires-at]').forEach(el=>{el.textContent=formatLeft(Date.parse(el.getAttribute('data-expires-at')||'')-Date.now())})}
function expiresFromLockDays(node){const row=node&&node.closest&&node.closest('.lock-row');const input=row&&row.querySelector('[data-lock-days]');const n=Number(input&&input.value||0);return n>0?new Date(Date.now()+n*86400000).toISOString():null}
setInterval(tickCountdowns,1000);
function renderUsers(){const q=userQuery.trim().toLowerCase();const rows=adminUsers.filter(u=>!q||String(u.id).toLowerCase().includes(q)||String(u.username).toLowerCase().includes(q)||String(u.firstName).toLowerCase().includes(q)||String(u.currentSection).toLowerCase().includes(q));if(!rows.length){usersList.innerHTML='<div class="empty">No users found.</div>';return}usersList.innerHTML=rows.map(u=>'<article class="user" id="user-'+esc(u.id)+'"><div class="user-top"><div class="user-name"><b>'+esc(u.username||'—')+'</b><span class="badge '+(u.isActive?'on':'off')+'">'+esc(u.status)+'</span></div><strong>'+fmt(u.credit)+'</strong></div><div class="user-info"><span>ID '+esc(u.id)+'</span><span>'+esc(u.firstName||'—')+'</span><span>'+esc(u.currentSection||'unknown')+'</span><span>'+ago(u.lastSeenAt)+'</span></div><button class="manage-btn" data-manage-user="'+esc(u.id)+'" type="button">Manage user</button><div class="user-manage" data-manage-panel="'+esc(u.id)+'" hidden></div></article>').join('');document.querySelectorAll('[data-manage-user]').forEach(btn=>btn.onclick=()=>toggleUserManage(btn.getAttribute('data-manage-user')))}
async function loadUsers(){usersStatus.textContent='Loading users...';try{const r=await fetch('/admin/api/users',{credentials:'same-origin'}),j=await r.json().catch(()=>({error:'Invalid response'}));if(!r.ok)throw new Error(j.error||'Could not load users');adminUsers=j.users||[];document.getElementById('statTotal').textContent=String(j.stats?.total??adminUsers.length);document.getElementById('statOnline').textContent=String(j.stats?.online??0);document.getElementById('statInactive').textContent=String(j.stats?.inactive??0);document.getElementById('statCredit').textContent=fmt(j.stats?.totalCredit??0);usersStatus.textContent='Updated '+new Date().toLocaleTimeString();renderUsers();if(openUser)renderUserControls(openUser)}catch(e){usersStatus.textContent=e.message||'Could not load users';usersList.innerHTML='<div class="empty">'+esc(e.message||'Error')+'</div>'}}
async function toggleUserManage(userId){openUser=openUser===userId?'':userId;document.querySelectorAll('.user-manage').forEach(p=>{p.hidden=true;p.innerHTML=''});if(openUser)renderUserControls(openUser)}
async function renderUserControls(userId){const panel=document.querySelector('[data-manage-panel="'+userId+'"]');if(!panel)return;panel.hidden=false;panel.innerHTML='<div class="mini-status">Loading controls...</div>';try{const r=await fetch('/admin/api/user-controls?userId='+encodeURIComponent(userId),{credentials:'same-origin'}),c=await r.json();if(!r.ok)throw new Error(c.error||'Could not load controls');const user=adminUsers.find(u=>String(u.id)===String(userId))||{};const credit=c.credit==null?Number(user.credit||0):Number(c.credit||0);const blocked=new Set(c.blockedSections||[]);panel.innerHTML='<div class="credit-tools"><button data-credit-adjust="-100">-100</button><input data-credit-value type="number" min="0" value="'+credit+'"/><button data-credit-adjust="100">+100</button></div><button class="save-credit" data-credit-save>Set credit</button><div class="section-user-blocks">'+Object.keys(sectionLabels).map(id=>'<button class="section-block '+(blocked.has(id)?'blocked':'')+'" data-user-section="'+id+'">'+sectionLabels[id]+' '+(blocked.has(id)?'Blocked':'Open')+'</button>').join('')+'</div><p class="mini-status" data-user-status></p>';panel.querySelectorAll('[data-credit-adjust]').forEach(btn=>btn.onclick=()=>adjustCredit(userId,Number(btn.getAttribute('data-credit-adjust')||0)));panel.querySelector('[data-credit-save]').onclick=()=>setCredit(userId,Number(panel.querySelector('[data-credit-value]').value||0));panel.querySelectorAll('[data-user-section]').forEach(btn=>btn.onclick=()=>blockSection(userId,btn.getAttribute('data-user-section'),!btn.classList.contains('blocked')))}catch(e){panel.innerHTML='<div class="mini-status">'+esc(e.message||'Could not load controls')+'</div>'}}
async function setCredit(userId,credit){const panel=document.querySelector('[data-manage-panel="'+userId+'"]'),status=panel&&panel.querySelector('[data-user-status]');if(status)status.textContent='Saving credit...';try{const r=await fetch('/admin/api/users/credit',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({userId,credit})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Could not save credit');if(status)status.textContent='Credit saved';await loadUsers()}catch(e){if(status)status.textContent=e.message||'Could not save credit'}}
async function adjustCredit(userId,delta){const panel=document.querySelector('[data-manage-panel="'+userId+'"]'),status=panel&&panel.querySelector('[data-user-status]');if(status)status.textContent='Adjusting credit...';try{const r=await fetch('/admin/api/users/credit-adjust',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({userId,delta})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Could not adjust credit');if(status)status.textContent='Credit adjusted';await loadUsers()}catch(e){if(status)status.textContent=e.message||'Could not adjust credit'}}
async function blockSection(userId,sectionId,blocked){const panel=document.querySelector('[data-manage-panel="'+userId+'"]'),status=panel&&panel.querySelector('[data-user-status]');if(status)status.textContent='Saving access...';try{const r=await fetch('/admin/api/users/section-block',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({userId,sectionId,blocked})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Could not update access');if(status)status.textContent='Access updated';renderUserControls(userId)}catch(e){if(status)status.textContent=e.message||'Could not update access'}}
function sharedImageUrl(kind){const first=sectionLocks[0]||{};return kind==='code'?first.codeImageUrl:first.lockedImageUrl}
function renderSharedImagePreviews(){const locked=document.getElementById('sharedLockedPreview'),code=document.getElementById('sharedCodePreview'),lockedUrl=sharedImageUrl('locked'),codeUrl=sharedImageUrl('code');if(locked)locked.innerHTML=lockedUrl?'<img src="'+esc(lockedUrl)+'&t='+Date.now()+'" alt=""/>':'<span>No normal lock image</span>';if(code)code.innerHTML=codeUrl?'<img src="'+esc(codeUrl)+'&t='+Date.now()+'" alt=""/>':'<span>No code lock image</span>'}
function renderLocks(){if(!sectionLocks.length){locksList.innerHTML='<div class="empty">No sections found.</div>';renderSharedImagePreviews();return}renderSharedImagePreviews();locksList.innerHTML=sectionLocks.map(s=>'<article class="lock-row"><div class="lock-main"><strong>'+esc(s.label)+'</strong><p>'+esc(s.description)+'</p><span class="lock-state">'+esc(s.mode==='code'?'Code locked':s.mode==='locked'?'Locked':'Open')+(s.expiresAt?' · <span data-expires-at="'+esc(s.expiresAt)+'">'+formatLeft(s.remainingMs)+'</span>':(s.mode!=='open'?' · forever':''))+(s.hasCode?' · code saved':'')+'</span></div><input data-lock-days type="number" min="0" step="0.01" placeholder="days, 0 forever"/><div class="lock-actions"><button class="lock-toggle '+(s.mode==='open'?'active':'')+'" data-lock-open="'+esc(s.id)+'">Open</button><button class="lock-toggle '+(s.mode==='locked'?'locked active':'')+'" data-lock-section="'+esc(s.id)+'">Lock</button></div><div class="code-lock"><input class="code-input" data-code-input="'+esc(s.id)+'" type="text" inputmode="text" placeholder="Access code"/><button class="lock-toggle code '+(s.mode==='code'?'active':'')+'" data-code-lock="'+esc(s.id)+'">Code lock</button></div></article>').join('');tickCountdowns();document.querySelectorAll('[data-lock-open]').forEach(btn=>btn.onclick=()=>toggleLock(btn.getAttribute('data-lock-open'),false,btn));document.querySelectorAll('[data-lock-section]').forEach(btn=>btn.onclick=()=>toggleLock(btn.getAttribute('data-lock-section'),true,btn));document.querySelectorAll('[data-code-lock]').forEach(btn=>btn.onclick=()=>setCodeLock(btn.getAttribute('data-code-lock'),btn));document.querySelectorAll('[data-upload-shared-lock-image]').forEach(btn=>btn.onclick=()=>uploadSharedLockImage(btn.getAttribute('data-upload-shared-lock-image')))}
async function loadLocks(force=false){if(locksLoaded&&!force){renderLocks();return}locksLoaded=true;locksStatus.textContent='Loading section locks...';try{const r=await fetch('/admin/api/section-locks',{credentials:'same-origin'}),j=await r.json().catch(()=>({error:'Invalid response'}));if(!r.ok)throw new Error(j.error||'Could not load locks');sectionLocks=j.sections||[];locksStatus.textContent='Updated '+new Date().toLocaleTimeString();renderLocks()}catch(e){locksStatus.textContent=e.message||'Could not load locks';locksList.innerHTML='<div class="empty">'+esc(e.message||'Error')+'</div>'}}
async function uploadSharedLockImage(kind){const input=document.querySelector('[data-shared-lock-image="'+kind+'"]');if(!input||!input.files||!input.files[0]){locksStatus.textContent='Choose an image first';return}if(!allowed.includes(input.files[0].type)){locksStatus.textContent='Only PNG, JPG, JPEG or WebP.';return}locksStatus.textContent='Uploading shared image...';const form=new FormData();form.append('sectionId','shared');form.append('kind',kind);form.append('image',input.files[0]);try{const r=await fetch('/admin/api/section-lock-image',{method:'POST',credentials:'same-origin',body:form});const j=await r.json().catch(()=>({error:'Invalid response'}));if(!r.ok)throw new Error(j.error||'Could not upload image');sectionLocks=j.sections||[];locksStatus.textContent='Shared image saved';input.value='';renderLocks()}catch(e){locksStatus.textContent=e.message||'Could not upload image'}}
async function toggleLock(sectionId,locked,source){locksStatus.textContent='Saving...';try{const expiresAt=locked?expiresFromLockDays(source):null;const r=await fetch('/admin/api/section-locks-timed',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({sectionId,locked,expiresAt})});const j=await r.json().catch(()=>({error:'Invalid response'}));if(!r.ok)throw new Error(j.error||'Could not save lock');sectionLocks=j.sections||[];locksStatus.textContent='Saved';renderLocks()}catch(e){locksStatus.textContent=e.message||'Could not save lock'}}
async function setCodeLock(sectionId,source){const input=document.querySelector('[data-code-input="'+sectionId+'"]');const code=(input&&input.value||'').trim();if(!code){locksStatus.textContent='Enter an access code first';return}locksStatus.textContent='Saving code lock...';try{const expiresAt=expiresFromLockDays(source||input);const r=await fetch('/admin/api/section-locks/code-timed',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({sectionId,code,expiresAt})});const j=await r.json().catch(()=>({error:'Invalid response'}));if(!r.ok)throw new Error(j.error||'Could not save code lock');sectionLocks=j.sections||[];locksStatus.textContent='Code lock saved';renderLocks()}catch(e){locksStatus.textContent=e.message||'Could not save code lock'}}
document.getElementById('refreshUsers').onclick=loadUsers;document.getElementById('refreshLocks').onclick=()=>loadLocks(true);search.oninput=()=>{userQuery=search.value;renderUsers()};document.querySelectorAll('[data-upload-shared-lock-image]').forEach(btn=>btn.onclick=()=>uploadSharedLockImage(btn.getAttribute('data-upload-shared-lock-image')));loadUsers();
</script></body></html>`;
}

function mobileAdminCss(): string {
  return `:root{color-scheme:dark;font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:#000;color:#fff}*{box-sizing:border-box;-webkit-tap-highlight-color:transparent}html,body{width:100%;min-height:100%;overflow-x:hidden}body{margin:0;background:#000;color:#fff;padding:calc(12px + env(safe-area-inset-top)) 12px calc(28px + env(safe-area-inset-bottom))}.page{width:100%;max-width:430px;margin:0 auto}.login{padding-top:13vh}.head,.row-title{display:flex;align-items:flex-start;justify-content:space-between;gap:10px}.head{margin-bottom:16px}.head-actions{display:flex;align-items:center;gap:8px;flex:0 0 auto}.eyebrow{margin:0 0 5px;color:rgba(255,255,255,.42);font-size:10px;font-weight:950;letter-spacing:.16em;text-transform:uppercase}h1{margin:0 0 4px;font-size:30px;line-height:.95;letter-spacing:-.075em}h2{margin:0 0 3px;font-size:17px;letter-spacing:-.04em}.muted{margin:0;color:rgba(255,255,255,.5);font-size:12px;line-height:1.35}.small-text{font-size:11px}.section{margin-top:22px}.admin-section{display:none}.admin-section.active{display:block}.icon-menu{width:31px;height:31px;border:1px solid rgba(255,255,255,.16);border-radius:999px;background:#070707;display:grid;place-content:center;gap:3px;padding:0}.icon-menu span{display:block;width:13px;height:1.5px;border-radius:999px;background:#fff}.menu-panel{margin:0 0 18px;padding:7px 0;border-top:1px solid rgba(255,255,255,.08);border-bottom:1px solid rgba(255,255,255,.08)}.menu-item{width:100%;border:0;background:transparent;color:#fff;text-align:left;padding:11px 0;display:grid;gap:3px;border-bottom:1px solid rgba(255,255,255,.06)}.menu-item:last-child{border-bottom:0}.menu-item strong{font-size:14px}.menu-item span{font-size:11px;color:rgba(255,255,255,.45)}.menu-item.active span{color:rgba(255,255,255,.72)}.stats{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin:15px 0 14px}.stats div{min-width:0;border-bottom:1px solid rgba(255,255,255,.1);padding-bottom:10px}.stats span{display:block;color:rgba(255,255,255,.42);font-size:9px;font-weight:950;text-transform:uppercase;letter-spacing:.1em}.stats b{display:block;margin-top:4px;font-size:19px;line-height:1;letter-spacing:-.05em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}label{display:block;margin:13px 0 7px;color:rgba(255,255,255,.48);font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:.12em}input{width:100%;height:42px;border-radius:999px;border:1px solid rgba(255,255,255,.16);background:#050505;color:#fff;padding:0 13px;font:inherit;font-size:14px;outline:none}input:focus{border-color:rgba(255,255,255,.38)}input[type=file]{height:auto;border-radius:15px;padding:11px;font-size:12px}.search{position:sticky;top:0;z-index:4;padding:1px 0 8px;background:linear-gradient(#000 75%,rgba(0,0,0,0))}.primary{width:100%;height:44px;border:0;border-radius:999px;background:#fff;color:#050505;font-weight:950;font-size:14px;margin-top:10px}.ghost{height:31px;border:1px solid rgba(255,255,255,.16);border-radius:999px;background:#070707;color:#fff;padding:0 10px;font-size:12px;font-weight:850;flex:0 0 auto}.status,.mini-status{min-height:16px;margin:8px 0 0;color:rgba(255,255,255,.55);font-size:11px}.users{display:grid;margin-top:2px}.user{padding:12px 0;border-bottom:1px solid rgba(255,255,255,.085)}.user:last-child{border-bottom:0}.user-top{display:flex;align-items:center;justify-content:space-between;gap:10px;min-width:0}.user-top>strong{font-size:14px;white-space:nowrap}.user-name{display:flex;align-items:center;gap:7px;min-width:0}.user-name b{font-size:14px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:210px}.badge{border-radius:999px;padding:4px 7px;font-size:9px;font-weight:950;flex:0 0 auto}.badge.on{background:#fff;color:#050505}.badge.off{background:rgba(255,255,255,.08);color:rgba(255,255,255,.55)}.user-info{display:grid;grid-template-columns:1fr 1fr;gap:3px 10px;margin-top:6px}.user-info span{min-width:0;color:rgba(255,255,255,.42);font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.manage-btn{width:100%;height:30px;margin-top:9px;border-radius:999px;border:1px solid rgba(255,255,255,.14);background:#070707;color:#fff;font-weight:850;font-size:11px}.user-manage{display:grid;gap:8px;margin-top:10px;padding-top:10px;border-top:1px solid rgba(255,255,255,.08)}.credit-tools{display:grid;grid-template-columns:62px 1fr 62px;gap:7px}.credit-tools button,.save-credit,.section-block{height:34px;border-radius:999px;border:1px solid rgba(255,255,255,.14);background:#070707;color:#fff;font-weight:900;font-size:11px}.credit-tools input{height:34px;text-align:center;font-weight:900}.save-credit{background:#fff;color:#050505}.section-user-blocks{display:grid;grid-template-columns:1fr 1fr;gap:7px}.section-block.blocked{border-color:rgba(255,255,255,.38);background:#fff;color:#050505}.empty{text-align:center;color:rgba(255,255,255,.42);padding:22px 0;font-size:13px}.image-current{display:flex;align-items:center;gap:10px;border-top:1px solid rgba(255,255,255,.09);border-bottom:1px solid rgba(255,255,255,.09);padding:14px 0;margin:16px 0}.image-current img{width:38px;height:38px;object-fit:contain}.locks-list{display:grid;margin-top:16px}.lock-row{display:grid;gap:10px;padding:13px 0;border-bottom:1px solid rgba(255,255,255,.085)}.lock-main strong{font-size:14px}.lock-main p{margin:4px 0 0;color:rgba(255,255,255,.45);font-size:11px}.lock-state{display:inline-block;margin-top:6px;color:rgba(255,255,255,.62);font-size:10px;font-weight:850;text-transform:uppercase;letter-spacing:.08em}.lock-actions{display:grid;grid-template-columns:1fr 1fr;gap:7px}.lock-toggle{height:31px;min-width:72px;border-radius:999px;border:1px solid rgba(255,255,255,.16);background:#070707;color:#fff;font-weight:950;font-size:11px}.lock-toggle.active{background:#fff;color:#050505}.lock-toggle.locked.active,.lock-toggle.code.active{background:#070707;color:#fff;border-color:rgba(255,255,255,.36)}.code-lock,.lock-image-upload{display:grid;grid-template-columns:1fr 94px;gap:7px}.code-input{height:34px;border-radius:999px;font-size:12px}.shared-lock-images{display:grid;gap:9px;margin:14px 0 6px;padding:10px 0;border-top:1px solid rgba(255,255,255,.08);border-bottom:1px solid rgba(255,255,255,.08)}.lock-preview{height:74px;border:1px solid rgba(255,255,255,.09);border-radius:18px;background:#050505;display:grid;place-items:center;color:rgba(255,255,255,.42);font-size:11px;overflow:hidden}.lock-preview img{width:100%;height:100%;object-fit:contain}.lock-image-upload input{height:34px;border-radius:999px;font-size:11px;padding:7px}.upload{padding-bottom:8px}@media(min-width:560px){.page{max-width:520px}.stats{grid-template-columns:repeat(4,minmax(0,1fr))}.user-info{grid-template-columns:1.2fr 1fr 1fr auto}.user-name b{max-width:260px}}`;
}
