import { mobileAdminLoginHtml, mobileAdminPanelHtml } from './admin-mobile';
import { ADMIN_IMAGE_PANEL_SCRIPT } from './admin-image-panel';
import { ADMIN_UPLOAD_CACHE_SCRIPT } from './admin-upload-cache-panel';
import { ADMIN_PLINKO_CONTROL_SCRIPT } from './admin-plinko-control-panel';
import { ADMIN_PLAY_ZONE_CARDS_PANEL_SCRIPT } from './admin-play-zone-cards-panel';
import { ADMIN_MARKET_PANEL_SCRIPT } from './admin-market-panel';
import { ADMIN_PREDICT_PANEL_SCRIPT } from './admin-predict-panel';
import { ADMIN_PREDICT_LOADER_PANEL_SCRIPT } from './admin-predict-loader-panel';
import { ADMIN_FOOTBALL_PANEL_SCRIPT } from './admin-football-panel';
import { ADMIN_TON_PANEL_SCRIPT } from './admin-ton-panel';
import { ADMIN_HOME_FINANCE_IMAGE_PANEL_SCRIPT } from './admin-home-finance-image-panel';
import { ADMIN_RANK_CHARACTER_PANEL_SCRIPT } from './admin-rank-character-panel';
import { ADMIN_AUDIO_PANEL_SCRIPT } from './admin-audio-panel';
import { ADMIN_GROUP_AI_PROVIDER_PANEL_SCRIPT } from './admin-group-ai-provider-panel';
import { ADMIN_FORCE_REFRESH_PANEL_SCRIPT } from './admin-force-refresh-panel';
import { ADMIN_NFT_PRICE_ICON_PANEL_SCRIPT } from './admin-nft-price-icon-panel';
import { ADMIN_MARKET_PROVIDER_PANEL_SCRIPT } from './admin-market-provider-panel';
import { ADMIN_DAILY_REWARDS_PANEL_SCRIPT } from './admin-daily-rewards-panel';
import { ADMIN_SECTION_LOADING_PANEL_SCRIPT } from './admin-section-loading-panel';
import { ADMIN_TRUSTED_ACCESS_PANEL_SCRIPT } from './admin-trusted-access-panel';

const ADMIN_LAYOUT_CSS = `<style>
.icon-menu{display:none!important}.menu-panel,.menu-panel[hidden]{display:flex!important;gap:7px!important;overflow-x:auto!important;margin:4px 0 18px!important;padding:0 0 8px!important;border:0!important;background:transparent!important;scrollbar-width:none}.menu-panel::-webkit-scrollbar{display:none}.menu-item{flex:0 0 auto!important;width:auto!important;min-width:86px!important;border:1px solid rgba(255,255,255,.14)!important;border-radius:999px!important;background:#070707!important;text-align:center!important;padding:8px 11px!important}.menu-item strong{display:block!important;font-size:12px!important;line-height:1!important}.menu-item span{display:none!important}.menu-item.active{background:#fff!important;color:#050505!important;border-color:#fff!important}.page{max-width:440px!important}.head{margin-bottom:10px!important}.head h1{font-size:28px!important}.head .muted{font-size:11px!important}.admin-section{margin-top:0!important}.row-title{margin-bottom:10px!important}.row-title h2,.admin-section h2{font-size:18px!important}.small-text{font-size:10px!important}.stats{gap:0!important;margin:13px 0!important;border-top:1px solid rgba(255,255,255,.09)!important;border-bottom:1px solid rgba(255,255,255,.09)!important}.stats div{border:0!important;border-right:1px solid rgba(255,255,255,.08)!important;border-radius:0!important;background:transparent!important;padding:9px 8px!important}.stats div:last-child{border-right:0!important}.stats span{font-size:8px!important}.stats b{font-size:17px!important;margin-top:4px!important}input{height:38px!important;font-size:13px!important}.status,.mini-status{font-size:11px!important}.users{display:block!important}.user{padding:12px 0!important;border-bottom:1px solid rgba(255,255,255,.11)!important}.user:first-child{border-top:1px solid rgba(255,255,255,.11)!important}.user-name b{font-size:15px!important}.badge{font-size:9px!important;padding:4px 7px!important}.user-top>strong{font-size:16px!important}.user-info{grid-template-columns:1fr 1fr!important;margin-top:7px!important}.user-info span{font-size:11px!important;color:rgba(255,255,255,.46)!important}.manage-btn{height:31px!important;margin-top:9px!important;font-size:12px!important}.user-manage{margin-top:9px!important;padding-top:9px!important}.credit-tools button,.credit-tools input,.section-block{height:34px!important;font-size:12px!important}.credit-tools button{font-size:20px!important;line-height:1!important}.locks-list{display:block!important}.lock-row{padding:12px 0!important;border-bottom:1px solid rgba(255,255,255,.11)!important}.lock-row:first-child{border-top:1px solid rgba(255,255,255,.11)!important}.lock-main strong{font-size:15px!important}.lock-main p{font-size:11px!important}.lock-preview{height:62px!important}.lock-toggle{height:32px!important;font-size:11px!important}.image-current{padding:12px 0!important}.image-current img{width:40px!important}.plinko-simple-card{padding:10px 0!important}.plinko-house-row{padding:7px 0!important}.plinko-house-row input{height:32px!important}.plinko-control-actions .ghost,.preset-row button{height:32px!important}.reset-user-btn{height:31px!important;margin-top:8px!important;border:0!important;border-radius:999px!important;background:rgba(255,80,80,.16)!important;color:#ffb3b3!important;font-size:12px!important;width:100%!important}
</style>`;

const ADMIN_GROUPED_USERS_SCRIPT = `<script>(function(){
  var activeSource='ai_bot', usersCache=[];
  var sectionLabels={home:'Home',connect:'Connect',flow:'TTS',playzone:'Play Zone',predict:'Predict',market:'Market'};
  function esc(v){return String(v??'').replace(/[&<>]/g,function(s){return {'&':'&amp;','<':'&lt;','>':'&gt;'}[s]||s})}
  function fmt(v){return Number(v||0).toLocaleString('en-US')}
  function ago(v){if(!v)return '—';var t=Date.parse(v);if(!t)return v;var d=Math.max(0,Date.now()-t);if(d<60000)return 'now';if(d<3600000)return Math.floor(d/60000)+'m';if(d<86400000)return Math.floor(d/86400000)+'d';return Math.floor(d/86400000)+'d'}
  function title(src){return src==='ai_bot'?'AI Bot Users':src==='game_bot'?'Game Bot Users':src==='user_bot'?'User Bot Users':'Other Users'}
  function money(u){return u.tonBalance||fmt(u.tonBalanceNano||u.credit||0)}
  function openUsersSource(src){activeSource=src||'ai_bot';document.querySelectorAll('.menu-item').forEach(function(x){x.classList.toggle('active',x.getAttribute('data-user-menu')===activeSource)});document.querySelectorAll('.admin-section').forEach(function(s){s.classList.toggle('active',s.id==='sectionUsers')});var titleEl=document.getElementById('adminTitle'),sub=document.getElementById('adminSubtitle');if(titleEl)titleEl.textContent=title(activeSource);if(sub)sub.textContent='Manage '+title(activeSource).toLowerCase()+'.';renderList();window.scrollTo({top:0,behavior:'smooth'})}
  function installMenu(){var menu=document.getElementById('adminMenu');if(!menu||document.getElementById('aiUsersMenu'))return;var original=menu.querySelector('[data-section="users"]');if(original)original.remove();menu.insertAdjacentHTML('afterbegin','<button class="menu-item active" id="aiUsersMenu" data-user-menu="ai_bot" type="button"><strong>AI Bot Users</strong><span>Users of AI bot</span></button><button class="menu-item" data-user-menu="game_bot" type="button"><strong>Game Bot Users</strong><span>Users of game bot</span></button><button class="menu-item" data-user-menu="user_bot" type="button"><strong>User Bots</strong><span>Users of connected bots</span></button>');document.querySelectorAll('[data-user-menu]').forEach(function(b){b.onclick=function(){openUsersSource(b.getAttribute('data-user-menu')||'ai_bot')}})}
  function row(u){return '<article class="user" id="user-'+esc(u.id)+'"><div class="user-top"><div class="user-name"><b>'+esc(u.username||'—')+'</b><span class="badge '+(u.isActive?'on':'off')+'">'+esc(u.status||'Inactive')+'</span></div><strong>'+esc(money(u))+'</strong></div><div class="user-info"><span>ID '+esc(u.id)+'</span><span>'+esc(u.firstName||'—')+'</span><span>'+esc(u.currentSection||u.sourceLabel||title(u.source))+'</span><span>'+ago(u.lastSeenAt)+'</span></div><button class="manage-btn" data-new-manage="'+esc(u.id)+'" type="button">Manage user</button><button class="reset-user-btn" data-reset-user="'+esc(u.id)+'" type="button">Reset user</button><div class="user-manage" data-new-panel="'+esc(u.id)+'" hidden></div></article>'}
  function renderList(){var list=document.getElementById('usersList'),search=document.getElementById('userSearch');if(!list)return;var q=String(search&&search.value||'').trim().toLowerCase();var rows=usersCache.filter(function(u){return (u.source||'unknown')===activeSource}).filter(function(u){return !q||String(u.id).toLowerCase().includes(q)||String(u.username).toLowerCase().includes(q)||String(u.firstName).toLowerCase().includes(q)||String(u.currentSection).toLowerCase().includes(q)});list.innerHTML=rows.length?rows.map(row).join(''):'<div class="empty">No users in '+title(activeSource)+'.</div>';document.querySelectorAll('[data-new-manage]').forEach(function(btn){btn.onclick=function(){toggleManage(btn.getAttribute('data-new-manage'))}});document.querySelectorAll('[data-reset-user]').forEach(function(btn){btn.onclick=function(){resetUser(btn.getAttribute('data-reset-user'))}})}
  async function renderGrouped(){var status=document.getElementById('usersStatus');try{installMenu();if(status)status.textContent='Loading users...';var r=await fetch('/admin/api/users',{credentials:'same-origin'}),j=await r.json().catch(function(){return{error:'Invalid response'}});if(!r.ok)throw new Error(j.error||'Could not load users');usersCache=j.users||[];if(document.getElementById('statTotal'))document.getElementById('statTotal').textContent=String(j.stats&&j.stats.total||usersCache.length);if(document.getElementById('statOnline'))document.getElementById('statOnline').textContent=String(j.stats&&j.stats.online||0);if(document.getElementById('statInactive'))document.getElementById('statInactive').textContent=String(j.stats&&j.stats.inactive||0);if(document.getElementById('statCredit'))document.getElementById('statCredit').textContent=fmt(j.stats&&j.stats.totalTonBalanceNano||0);if(status)status.textContent=title(activeSource)+' updated '+new Date().toLocaleTimeString();openUsersSource(activeSource)}catch(e){if(status)status.textContent=e.message||'Could not load users'}}
  async function toggleManage(userId){document.querySelectorAll('[data-new-panel]').forEach(function(p){if(p.getAttribute('data-new-panel')!==userId){p.hidden=true;p.innerHTML=''}});var p=document.querySelector('[data-new-panel="'+userId+'"]');if(!p)return;if(!p.hidden){p.hidden=true;p.innerHTML='';return}p.hidden=false;p.innerHTML='<div class="mini-status">Loading controls...</div>';try{var r=await fetch('/admin/api/user-controls?userId='+encodeURIComponent(userId),{credentials:'same-origin'}),c=await r.json().catch(function(){return{error:'Invalid response'}});if(!r.ok)throw new Error(c.error||'Could not load controls');var bal=Number(c.tonBalanceNano||c.credit||0);var blocked=new Set(c.blockedSections||[]);p.innerHTML='<div class="credit-tools"><button data-adj="-100000000">-</button><input data-bal type="number" min="0" value="'+bal+'"/><button data-adj="100000000">+</button></div><button class="save-credit" data-save-bal>Set TON nano</button><div class="section-user-blocks">'+Object.keys(sectionLabels).map(function(id){return '<button class="section-block '+(blocked.has(id)?'blocked':'')+'" data-block="'+id+'">'+sectionLabels[id]+' '+(blocked.has(id)?'Blocked':'Open')+'</button>'}).join('')+'</div><p class="mini-status" data-st></p>';p.querySelector('[data-save-bal]').onclick=function(){setBalance(userId,Number(p.querySelector('[data-bal]').value||0),p)};p.querySelectorAll('[data-adj]').forEach(function(b){b.onclick=function(){adjustBalance(userId,Number(b.getAttribute('data-adj')||0),p)}});p.querySelectorAll('[data-block]').forEach(function(b){b.onclick=function(){blockSection(userId,b.getAttribute('data-block'),!b.classList.contains('blocked'),p)}})}catch(e){p.innerHTML='<div class="mini-status">'+esc(e.message||'Could not load controls')+'</div>'}}
  async function setBalance(userId,value,p){var st=p.querySelector('[data-st]');if(st)st.textContent='Saving...';try{var r=await fetch('/admin/api/users/ton-balance',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({userId:userId,tonBalanceNano:value})});var j=await r.json().catch(function(){return{error:'Invalid response'}});if(!r.ok)throw new Error(j.error||'Could not save balance');if(st)st.textContent='Saved';await renderGrouped()}catch(e){if(st)st.textContent=e.message||'Could not save'}}
  async function adjustBalance(userId,delta,p){var st=p.querySelector('[data-st]');if(st)st.textContent='Adjusting...';try{var r=await fetch('/admin/api/users/ton-balance-adjust',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({userId:userId,deltaNano:delta})});var j=await r.json().catch(function(){return{error:'Invalid response'}});if(!r.ok)throw new Error(j.error||'Could not adjust');if(st)st.textContent='Adjusted';await toggleManage(userId);await toggleManage(userId);await renderGrouped()}catch(e){if(st)st.textContent=e.message||'Could not adjust'}}
  async function blockSection(userId,sectionId,blocked,p){var st=p.querySelector('[data-st]');if(st)st.textContent='Saving access...';try{var r=await fetch('/admin/api/users/section-block',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({userId:userId,sectionId:sectionId,blocked:blocked})});var j=await r.json().catch(function(){return{error:'Invalid response'}});if(!r.ok)throw new Error(j.error||'Could not update access');await toggleManage(userId);await toggleManage(userId)}catch(e){if(st)st.textContent=e.message||'Could not update access'}}
  async function resetUser(userId){if(!userId)return;if(!confirm('Reset this user? '+userId))return;var status=document.getElementById('usersStatus');if(status)status.textContent='Resetting user...';try{var r=await fetch('/admin/api/users/'+encodeURIComponent(userId),{method:'DELETE',credentials:'same-origin'}),j=await r.json().catch(function(){return{error:'Invalid response'}});if(!r.ok)throw new Error(j.error||'Could not reset user');await renderGrouped()}catch(e){if(status)status.textContent=e.message||'Could not reset user'}}
  window.VexaRenderGroupedUsers=renderGrouped;setTimeout(renderGrouped,250);setTimeout(renderGrouped,900);var s=document.getElementById('userSearch');if(s)s.addEventListener('input',function(){setTimeout(renderList,30)});var rr=document.getElementById('refreshUsers');if(rr)rr.addEventListener('click',function(){setTimeout(renderGrouped,30)},true);
})();</script>`;

function adminPanelWithFixes(): string {
  return mobileAdminPanelHtml()
    .replace(/Credit and per-user access/g, 'TON Balance and per-user access')
    .replace(/Credit and section access/g, 'TON Balance and section access')
    .replace(/Manage credit/g, 'Manage TON balance')
    .replace('loadUsers();loadLocks();setInterval(loadUsers,15000);', 'loadUsers();loadLocks();');
}

export function adminHtml(): string {
  return mobileAdminLoginHtml();
}

export function adminPanelHtml(): string {
  return adminPanelWithFixes().replace('</body></html>', ADMIN_LAYOUT_CSS + ADMIN_GROUPED_USERS_SCRIPT + ADMIN_GROUP_AI_PROVIDER_PANEL_SCRIPT + ADMIN_TON_PANEL_SCRIPT + ADMIN_FORCE_REFRESH_PANEL_SCRIPT + ADMIN_NFT_PRICE_ICON_PANEL_SCRIPT + ADMIN_MARKET_PROVIDER_PANEL_SCRIPT + ADMIN_DAILY_REWARDS_PANEL_SCRIPT + ADMIN_IMAGE_PANEL_SCRIPT + ADMIN_HOME_FINANCE_IMAGE_PANEL_SCRIPT + ADMIN_RANK_CHARACTER_PANEL_SCRIPT + ADMIN_AUDIO_PANEL_SCRIPT + ADMIN_PLAY_ZONE_CARDS_PANEL_SCRIPT + ADMIN_MARKET_PANEL_SCRIPT + ADMIN_PREDICT_PANEL_SCRIPT + ADMIN_PREDICT_LOADER_PANEL_SCRIPT + ADMIN_FOOTBALL_PANEL_SCRIPT + ADMIN_SECTION_LOADING_PANEL_SCRIPT + ADMIN_TRUSTED_ACCESS_PANEL_SCRIPT + ADMIN_UPLOAD_CACHE_SCRIPT + ADMIN_PLINKO_CONTROL_SCRIPT + '</body></html>');
}

export function defaultCreditIconSvg(): string {
  return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64"><rect width="64" height="64" rx="32" fill="black"/><circle cx="32" cy="32" r="22" fill="white"/></svg>`;
}
