{
  "name": "ai-builder-tel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply AI_BUILDER_TEL_DB --local",
    "db:migrate:prod": "wrangler d1 migrations apply AI_BUILDER_TEL_DB --remote",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.0",
    "hono": "^4.7.11",
    "zod": "^3.25.64"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250607.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.0"
  }
}
